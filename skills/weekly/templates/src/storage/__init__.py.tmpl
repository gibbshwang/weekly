"""Storage abstraction for weekly_runtime.

Phase 0: LocalStorage (single-PC dogfood).
Phase 1: SMBStorage (UNC-path / shared filesystem).
"""
from .base import Storage
from .local import LocalStorage
from .smb import SMBStorage

__all__ = ["Storage", "LocalStorage", "SMBStorage"]
