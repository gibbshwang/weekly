---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/lib/firebase.ts
  - src/lib/firebaseAdmin.ts
  - src/components/AuthProvider.tsx
  - src/types/law.ts
  - src/types/analysis.ts
  - vitest.config.ts
  - src/__tests__/setup.ts
  - .env.example
  - .env.local
  - .gitignore
autonomous: true
requirements:
  - INFRA-03
  - AUTH-01
  - PRIV-01
  - PRIV-02

must_haves:
  truths:
    - "Next.js 15 dev server starts without errors"
    - "Firebase anonymous auth initializes on first page visit"
    - "Vitest runs and reports 0 failures"
    - "No wizard data persists to localStorage or sessionStorage"
  artifacts:
    - path: "src/lib/firebase.ts"
      provides: "Client-side Firebase init with window guard"
      contains: "typeof window"
    - path: "src/lib/firebaseAdmin.ts"
      provides: "Server-side Firebase Admin with lazy Proxy pattern"
      contains: "new Proxy"
    - path: "src/components/AuthProvider.tsx"
      provides: "Anonymous auth on mount"
      contains: "signInAnonymously"
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
      contains: "jsdom"
    - path: "src/types/law.ts"
      provides: "Type definitions for law API responses"
      exports: ["Statute", "Precedent"]
  key_links:
    - from: "src/components/AuthProvider.tsx"
      to: "src/lib/firebase.ts"
      via: "import { auth }"
      pattern: "import.*auth.*from.*firebase"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/AuthProvider.tsx"
      via: "wraps children"
      pattern: "<AuthProvider>"
---

<objective>
Next.js 15 프로젝트를 스캐폴딩하고, Firebase 익명 인증, 공유 타입 정의, 테스트 인프라를 구축한다.

Purpose: 이후 모든 Plan이 의존하는 프로젝트 기반 — 패키지 설치, TypeScript 설정, Firebase 초기화 패턴, Vitest 설정이 없으면 아무것도 시작할 수 없다.
Output: 작동하는 Next.js 15 dev server + Firebase anonymous auth + Vitest 실행 환경 + 공유 타입 정의
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Next.js 15 프로젝트 스캐폴딩 + 패키지 설치 + 테스트 인프라</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/globals.css, src/app/layout.tsx, vitest.config.ts, src/__tests__/setup.ts, .env.example, .env.local, .gitignore</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack 섹션: 패키지 버전, 설치 커맨드)
  </read_first>
  <action>
1. `npx create-next-app@15 . --typescript --tailwind --app --src-dir --no-import-alias --no-eslint` 실행하여 Next.js 15 프로젝트 생성. 이미 파일이 존재하면 `--yes` 플래그로 덮어쓰기.

2. 패키지 설치 (RESEARCH.md Installation 섹션 그대로):
```bash
npm install next@15 react@19 react-dom@19 typescript
npm install @anthropic-ai/sdk
npm install firebase firebase-admin
npm install fast-xml-parser
npm install zustand next-intl
npm install zod
npm install -D tailwindcss @tailwindcss/postcss vitest @vitejs/plugin-react \
  @testing-library/react @testing-library/user-event jsdom \
  @types/node @types/react @types/react-dom eslint eslint-config-next prettier
```

3. `tsconfig.json`에 strict mode 활성화:
```json
{
  "compilerOptions": {
    "strict": true,
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "jsx": "preserve",
    "module": "esnext",
    "moduleResolution": "bundler",
    "paths": { "@/*": ["./src/*"] },
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "incremental": true,
    "plugins": [{ "name": "next" }]
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

4. `postcss.config.mjs` — Tailwind v4 CSS-first:
```javascript
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
```

5. `src/app/globals.css` — Tailwind v4 import + 디자인 토큰:
```css
@import "tailwindcss";

@theme {
  --color-warm-gray: #F5F3F0;
  --color-deep-teal: #1B6B5A;
  --color-deep-teal-dark: #155A4A;
  --font-family-pretendard: 'Pretendard', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
}
```

6. `src/app/layout.tsx` — 루트 레이아웃 (Pretendard CDN import, globals.css import):
```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: '생활 법률 안내',
  description: '상황에 맞는 법률 정보를 정리해 드립니다.',
  robots: { index: false, follow: false },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html>
      <head>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard/dist/web/static/pretendard.min.css" />
      </head>
      <body style={{ fontFamily: 'Pretendard, sans-serif' }}>
        {children}
      </body>
    </html>
  );
}
```

7. `vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/__tests__/setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

8. `src/__tests__/setup.ts`:
```typescript
import '@testing-library/jest-dom/vitest';
```
(jest-dom/vitest가 없으면 빈 파일로 생성하고 주석으로 "// global test setup" 남김)

9. `.env.example` 파일 생성:
```
# Firebase Client (NEXT_PUBLIC_ prefix = 클라이언트 노출 허용)
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin (서버 전용)
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
FIREBASE_ADMIN_PRIVATE_KEY=

# Claude API (서버 전용)
ANTHROPIC_API_KEY=

# 법제처 Open API (서버 전용)
KOREAN_LAW_OC_KEY=

# Vercel Cron (서버 전용)
CRON_SECRET=
```

10. `.env.local` 파일 생성 (KOREAN_LAW_OC_KEY만 실제 값 입력):
```
KOREAN_LAW_OC_KEY=oneday24n1
```

11. `.gitignore`에 `.env.local` 추가 확인. 없으면 추가.

12. `next.config.ts` — INFRA-03 runtime split 문서화:
```typescript
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Route Handlers에서 runtime 선언:
  // - 'nodejs': Claude streaming + Firebase Admin + fast-xml-parser (Node.js API 필요)
  // - 'edge': 정적 페이지 (기본값, 선언 불필요)
};

export default nextConfig;
```
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && npx next build 2>&1 | head -20 && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next": "15 (15.x 범위)
    - package.json contains "@anthropic-ai/sdk"
    - package.json contains "firebase"
    - package.json contains "fast-xml-parser"
    - package.json contains "vitest"
    - tsconfig.json contains "strict": true
    - postcss.config.mjs contains "@tailwindcss/postcss"
    - src/app/globals.css contains "@import \"tailwindcss\""
    - src/app/globals.css contains "--color-warm-gray: #F5F3F0"
    - src/app/globals.css contains "--color-deep-teal: #1B6B5A"
    - vitest.config.ts contains "environment: 'jsdom'"
    - .env.example contains "ANTHROPIC_API_KEY="
    - .env.example contains "KOREAN_LAW_OC_KEY="
    - .env.local contains "KOREAN_LAW_OC_KEY=oneday24n1"
    - .gitignore contains ".env.local"
    - next.config.ts exists
  </acceptance_criteria>
  <done>Next.js 15 dev server가 에러 없이 시작되고, Vitest가 실행 가능하며, 모든 의존성이 설치됨</done>
</task>

<task type="auto">
  <name>Task 2: Firebase 초기화 + AuthProvider + 공유 타입 정의</name>
  <files>src/lib/firebase.ts, src/lib/firebaseAdmin.ts, src/components/AuthProvider.tsx, src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx, src/types/law.ts, src/types/analysis.ts</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Firebase 초기화, Pattern 2: 법제처 API 래퍼 타입)
    - src/app/layout.tsx (Task 1에서 생성한 루트 레이아웃 — 현재 상태 확인)
  </read_first>
  <action>
1. `src/lib/firebase.ts` — 클라이언트 SDK 초기화 (window guard 패턴, AUTH-01 + PRIV-01):
```typescript
import { initializeApp, getApps, getApp, type FirebaseApp } from 'firebase/app';
import { getAuth, type Auth } from 'firebase/auth';

const firebaseConfig = {
  apiKey: process.env.NEXT_PUBLIC_FIREBASE_API_KEY,
  authDomain: process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,
  projectId: process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,
  storageBucket: process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,
  messagingSenderId: process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,
  appId: process.env.NEXT_PUBLIC_FIREBASE_APP_ID,
};

let app: FirebaseApp | undefined;
let auth: Auth | undefined;

if (typeof window !== 'undefined') {
  app = getApps().length === 0 ? initializeApp(firebaseConfig) : getApp();
  auth = getAuth(app);
}

export { app, auth };
```

2. `src/lib/firebaseAdmin.ts` — Admin SDK Lazy Proxy 패턴 (서버 전용):
```typescript
import { initializeApp, getApps, cert, type App } from 'firebase-admin/app';
import { getFirestore, type Firestore } from 'firebase-admin/firestore';
import { getAuth, type Auth } from 'firebase-admin/auth';

function requireAdminApp(): App {
  if (getApps().length > 0) return getApps()[0];
  return initializeApp({
    credential: cert({
      projectId: process.env.FIREBASE_ADMIN_PROJECT_ID,
      clientEmail: process.env.FIREBASE_ADMIN_CLIENT_EMAIL,
      privateKey: process.env.FIREBASE_ADMIN_PRIVATE_KEY?.replace(/\\n/g, '\n'),
    }),
  });
}

export const adminDb: Firestore = new Proxy({} as Firestore, {
  get(_target, prop) {
    const db = getFirestore(requireAdminApp());
    const value = (db as unknown as Record<string | symbol, unknown>)[prop];
    return typeof value === 'function' ? (value as Function).bind(db) : value;
  },
});

export const adminAuth: Auth = new Proxy({} as Auth, {
  get(_target, prop) {
    const a = getAuth(requireAdminApp());
    const value = (a as unknown as Record<string | symbol, unknown>)[prop];
    return typeof value === 'function' ? (value as Function).bind(a) : value;
  },
});
```

3. `src/components/AuthProvider.tsx` — 익명 인증 자동 실행 (AUTH-01, PRIV-01):
```tsx
'use client';

import { useEffect, useState } from 'react';
import { auth } from '@/lib/firebase';
import { signInAnonymously, onAuthStateChanged, type User } from 'firebase/auth';

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (!auth) return;
    const unsubscribe = onAuthStateChanged(auth, (firebaseUser) => {
      setUser(firebaseUser);
      setLoading(false);
    });
    // 익명 인증 (아직 로그인 안 된 경우)
    if (!auth.currentUser) {
      signInAnonymously(auth).catch(console.error);
    }
    return () => unsubscribe();
  }, []);

  return <>{children}</>;
}
```

4. `src/types/law.ts` — 법제처 API 응답 타입 정의:
```typescript
/** 법령 검색 결과 단위 */
export interface Statute {
  name: string;        // 법령명한글
  mst: string;         // 법령MST (법령 고유 식별자)
  category: string;    // 법령구분명 (법률, 시행령 등)
  effectiveDate: string; // 시행일자
}

/** 판례 검색 결과 단위 */
export interface Precedent {
  caseNumber: string;  // 사건번호
  date: string;        // 선고일자
  summary: string;     // 판시사항 (HTML 스트립 후)
  ruling: string;      // 판결요지 (HTML 스트립 후)
  precSeq?: string;    // 판례일련번호 (상세 조회용)
}

/** 법령 조문 상세 */
export interface StatuteArticle {
  articleNumber: string; // 조문 번호
  articleTitle: string;  // 조문 제목
  articleContent: string; // 조문 내용 (HTML 스트립 후)
}

/** 법제처 API 검색 파라미터 */
export interface LawSearchParams {
  query: string;
  target: 'law' | 'prec';
  display?: number;    // 결과 수 (기본 5)
}
```

5. `src/types/analysis.ts` — 분석 파이프라인 타입 정의:
```typescript
import type { Statute, Precedent } from './law';

/** 위저드에서 수집하는 사용자 상황 데이터 */
export interface UserSituation {
  marriageDuration: string;
  hasChildren: boolean;
  childrenInfo?: string;
  assetOverview?: string;
  divorceReason: string;
  additionalContext?: string;
}

/** 분석 결과 구조 (Claude 출력) */
export interface AnalysisResult {
  issueChecklist: string[];       // 쟁점 체크리스트
  relevantStatutes: Statute[];    // 관련 법령
  relevantPrecedents: Precedent[]; // 관련 판례
  lawyerQuestions: string[];      // 변호사에게 물어볼 질문
  disclaimer: string;             // 면책 고지
}

/** 스트리밍 청크 타입 */
export interface AnalysisStreamChunk {
  type: 'text' | 'error' | 'done';
  content: string;
}
```

6. `src/app/[locale]/layout.tsx` — locale 레이아웃 (AuthProvider 래핑, SAFE-02 중립 메타데이터):
```tsx
import type { Metadata } from 'next';
import { AuthProvider } from '@/components/AuthProvider';

export const metadata: Metadata = {
  title: '생활 법률 안내',
  description: '상황에 맞는 법률 정보를 정리해 드립니다.',
  robots: { index: false, follow: false },
};

export default function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  return (
    <AuthProvider>
      <main className="min-h-screen bg-warm-gray font-[Pretendard]">
        {children}
      </main>
    </AuthProvider>
  );
}
```

7. `src/app/[locale]/page.tsx` — 임시 랜딩 페이지 (빌드 확인용):
```tsx
export default function HomePage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <h1 className="text-2xl text-deep-teal font-bold">생활 법률 안내</h1>
    </div>
  );
}
```

**PRIV-02**: Zustand store에 `persist` 미들웨어를 절대 사용하지 않는다. 위저드 데이터는 React 메모리에만 유지. localStorage/sessionStorage에 사용자 데이터를 쓰는 코드를 추가하지 않는다.
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && grep -l "signInAnonymously" src/components/AuthProvider.tsx && grep -l "typeof window" src/lib/firebase.ts && grep -l "new Proxy" src/lib/firebaseAdmin.ts && grep -l "export interface Statute" src/types/law.ts && grep -l "export interface AnalysisResult" src/types/analysis.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/firebase.ts contains "typeof window !== 'undefined'"
    - src/lib/firebase.ts contains "signInAnonymously" is NOT present (that's in AuthProvider)
    - src/lib/firebase.ts contains "getAuth(app)"
    - src/lib/firebaseAdmin.ts contains "new Proxy({} as Firestore"
    - src/lib/firebaseAdmin.ts contains "requireAdminApp()"
    - src/components/AuthProvider.tsx contains "signInAnonymously(auth)"
    - src/components/AuthProvider.tsx contains "'use client'"
    - src/types/law.ts contains "export interface Statute"
    - src/types/law.ts contains "export interface Precedent"
    - src/types/analysis.ts contains "export interface UserSituation"
    - src/types/analysis.ts contains "export interface AnalysisResult"
    - src/app/[locale]/layout.tsx contains "<AuthProvider>"
    - src/app/[locale]/layout.tsx does NOT contain "이혼" (SAFE-02)
    - No file in src/ contains "localStorage.setItem" (PRIV-02)
  </acceptance_criteria>
  <done>Firebase anonymous auth가 클라이언트에서 초기화되고, Admin SDK가 Lazy Proxy로 서버 전용 동작하며, 법률/분석 공유 타입이 정의됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Server | 클라이언트에서 서버 Route Handler로 전송되는 사용자 입력 |
| Server -> Firebase | Firebase Admin SDK를 통한 Firestore/Auth 접근 |
| Server -> Law API | 법제처 Open API로의 서버 사이드 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env.local | mitigate | .gitignore에 .env.local 포함. NEXT_PUBLIC_ prefix만 클라이언트 노출. KOREAN_LAW_OC_KEY, ANTHROPIC_API_KEY는 서버 전용. |
| T-01-02 | Information Disclosure | firebase.ts | mitigate | Firebase client config는 NEXT_PUBLIC_ 환경변수로만 노출. Security Rules로 데이터 접근 제한 (Phase 4). |
| T-01-03 | Spoofing | AuthProvider | accept | Firebase anonymous auth는 약한 인증이지만 MVP에서 충분. 저장 기능(Phase 4)에서 Google auth로 강화. |
| T-01-04 | Information Disclosure | SAFE-02 metadata | mitigate | 페이지 타이틀과 메타 description에 "이혼" 포함 금지. robots noindex/nofollow 설정으로 검색 노출 차단. |
</threat_model>

<verification>
1. `npm run dev` 실행 시 Next.js 15 dev server 에러 없이 시작
2. `npx vitest run` 실행 시 test runner가 동작 (테스트 0개여도 OK)
3. `npx tsc --noEmit` 실행 시 TypeScript 에러 0개
4. `src/types/law.ts`와 `src/types/analysis.ts`에서 타입 export 확인
</verification>

<success_criteria>
- Next.js 15 프로젝트가 빌드 성공
- Firebase 클라이언트/Admin SDK 초기화 코드가 형제 프로젝트 패턴을 따름
- 공유 타입 정의가 다음 Plan(02, 03, 04)에서 import 가능
- Vitest 실행 환경이 준비됨
- .env.example에 모든 필요 환경변수가 문서화됨
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
