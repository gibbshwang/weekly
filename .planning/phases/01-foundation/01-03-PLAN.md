---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/DisclaimerBanner.tsx
  - src/components/LawyerCTA.tsx
  - src/components/QuickExitButton.tsx
  - src/components/EmergencyContacts.tsx
  - src/components/IncognitoGuidance.tsx
  - src/components/PrivacyNotice.tsx
  - src/components/__tests__/DisclaimerBanner.test.tsx
  - src/components/__tests__/QuickExitButton.test.tsx
  - src/components/__tests__/EmergencyContacts.test.tsx
autonomous: true
requirements:
  - COMPL-01
  - COMPL-02
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-05
  - PRIV-04

must_haves:
  truths:
    - "모든 페이지 상단에 면책 고지 배너가 항상 표시된다"
    - "모든 분석 출력 근처에 변호사 상담 권유 CTA가 표시된다"
    - "화면 우상단에 긴급 탈출 버튼이 고정되어 있다"
    - "긴급 탈출 버튼 클릭 시 history.replaceState + sessionStorage.clear 후 중립 사이트로 이동한다"
    - "모든 페이지에서 긴급연락처(1366, 112)가 보인다"
    - "첫 방문 시 시크릿 모드 안내가 표시된다"
    - "사용자 데이터가 AI 학습에 사용되지 않는다는 안내가 존재한다"
  artifacts:
    - path: "src/components/DisclaimerBanner.tsx"
      provides: "면책 고지 배너 (COMPL-01)"
      contains: "법률 자문이 아닙니다"
    - path: "src/components/LawyerCTA.tsx"
      provides: "변호사 상담 권유 CTA (COMPL-02)"
      contains: "변호사 상담"
    - path: "src/components/QuickExitButton.tsx"
      provides: "긴급 탈출 버튼 (SAFE-01)"
      contains: "history.replaceState"
    - path: "src/components/EmergencyContacts.tsx"
      provides: "긴급연락처 1366/112 (SAFE-03)"
      contains: "1366"
    - path: "src/components/IncognitoGuidance.tsx"
      provides: "시크릿 모드 안내 (SAFE-05)"
      contains: "시크릿"
    - path: "src/components/PrivacyNotice.tsx"
      provides: "AI 학습 미사용 안내 (PRIV-04)"
      contains: "학습에 사용되지 않습니다"
  key_links:
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/DisclaimerBanner.tsx"
      via: "import and render above children"
      pattern: "<DisclaimerBanner"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/QuickExitButton.tsx"
      via: "import and render fixed position"
      pattern: "<QuickExitButton"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/EmergencyContacts.tsx"
      via: "import and render in footer area"
      pattern: "<EmergencyContacts"
---

<objective>
규제 준수(COMPL)와 DV 안전(SAFE) UI 컴포넌트를 모두 구현하고 locale 레이아웃에 배치한다.

Purpose: 변호사법 가드레일과 DV 피해자 안전은 Phase 1의 existential 요구사항. UI 없이는 서비스 출시 불가.
Output: DisclaimerBanner, LawyerCTA, QuickExitButton, EmergencyContacts, IncognitoGuidance, PrivacyNotice 컴포넌트 + 레이아웃 통합
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DisclaimerBanner + LawyerCTA + PrivacyNotice 컴포넌트</name>
  <files>src/components/DisclaimerBanner.tsx, src/components/LawyerCTA.tsx, src/components/PrivacyNotice.tsx, src/components/__tests__/DisclaimerBanner.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: Pitfall 3 — 면책고지 layout 배치 규칙)
  </read_first>
  <behavior>
    - DisclaimerBanner renders: "이 서비스는 법률 정보 제공 목적이며, 법률 자문이 아닙니다. 구체적인 법률 문제는 반드시 변호사와 상담하세요."
    - DisclaimerBanner has role="alert" and aria-live="polite"
    - LawyerCTA renders: "변호사 상담을 권유합니다" 텍스트 + 대한법률구조공단 링크 (https://www.klac.or.kr, tel:132)
    - LawyerCTA has 48px minimum touch target
    - PrivacyNotice renders: "입력하신 정보는 AI 모델 학습에 사용되지 않습니다"
  </behavior>
  <action>
1. `src/components/__tests__/DisclaimerBanner.test.tsx` — RED 테스트:
```tsx
import { render, screen } from '@testing-library/react';
import { DisclaimerBanner } from '../DisclaimerBanner';

describe('DisclaimerBanner', () => {
  it('renders disclaimer text', () => {
    render(<DisclaimerBanner />);
    expect(screen.getByText(/법률 자문이 아닙니다/)).toBeInTheDocument();
  });

  it('has alert role for accessibility', () => {
    render(<DisclaimerBanner />);
    expect(screen.getByRole('alert')).toBeInTheDocument();
  });

  it('includes lawyer consultation CTA text', () => {
    render(<DisclaimerBanner />);
    expect(screen.getByText(/변호사와 상담/)).toBeInTheDocument();
  });
});
```

2. `src/components/DisclaimerBanner.tsx` — GREEN 구현 (COMPL-01):
```tsx
export function DisclaimerBanner() {
  return (
    <div
      role="alert"
      aria-live="polite"
      className="w-full bg-amber-50 border-b border-amber-200 px-4 py-3 text-sm text-amber-900"
    >
      <p className="max-w-4xl mx-auto text-center">
        이 서비스는 법률 정보 제공 목적이며, <strong>법률 자문이 아닙니다</strong>.
        구체적인 법률 문제는 반드시 변호사와 상담하세요.
      </p>
    </div>
  );
}
```

3. `src/components/LawyerCTA.tsx` — (COMPL-02):
```tsx
export function LawyerCTA() {
  return (
    <div className="bg-deep-teal/5 border border-deep-teal/20 rounded-lg p-4 mt-6">
      <p className="text-deep-teal font-semibold mb-2">
        변호사 상담을 권유합니다
      </p>
      <p className="text-sm text-gray-700 mb-3">
        위 정보는 일반적인 법률 정보이며, 구체적인 상황에 대한 판단은 전문 변호사의 상담이 필요합니다.
      </p>
      <div className="flex flex-wrap gap-3">
        <a
          href="https://www.klac.or.kr"
          target="_blank"
          rel="noopener noreferrer"
          className="inline-flex items-center min-h-[48px] px-4 py-2 bg-deep-teal text-white rounded-lg text-sm font-medium hover:bg-deep-teal-dark transition-colors"
        >
          대한법률구조공단
        </a>
        <a
          href="tel:132"
          className="inline-flex items-center min-h-[48px] px-4 py-2 border border-deep-teal text-deep-teal rounded-lg text-sm font-medium hover:bg-deep-teal/5 transition-colors"
        >
          법률구조 상담전화 132
        </a>
      </div>
    </div>
  );
}
```

4. `src/components/PrivacyNotice.tsx` — (PRIV-04):
```tsx
export function PrivacyNotice() {
  return (
    <p className="text-xs text-gray-500 mt-4 text-center">
      입력하신 정보는 AI 모델 학습에 사용되지 않습니다.
      분석 완료 후 서버에 자동 저장되지 않으며, 브라우저를 닫으면 삭제됩니다.
    </p>
  );
}
```
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && npx vitest run src/components/__tests__/DisclaimerBanner.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DisclaimerBanner.tsx contains "법률 자문이 아닙니다"
    - src/components/DisclaimerBanner.tsx contains "role=\"alert\""
    - src/components/LawyerCTA.tsx contains "변호사 상담을 권유합니다"
    - src/components/LawyerCTA.tsx contains "https://www.klac.or.kr"
    - src/components/LawyerCTA.tsx contains "tel:132"
    - src/components/LawyerCTA.tsx contains "min-h-[48px]"
    - src/components/PrivacyNotice.tsx contains "AI 모델 학습에 사용되지 않습니다"
    - DisclaimerBanner test exits 0
  </acceptance_criteria>
  <done>면책 고지, 변호사 CTA, AI 학습 미사용 고지 컴포넌트가 모두 테스트와 함께 구현됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QuickExitButton + EmergencyContacts + IncognitoGuidance 컴포넌트</name>
  <files>src/components/QuickExitButton.tsx, src/components/EmergencyContacts.tsx, src/components/IncognitoGuidance.tsx, src/components/__tests__/QuickExitButton.test.tsx, src/components/__tests__/EmergencyContacts.test.tsx</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: DV 긴급 탈출, Pitfall 4: z-index 문제)
  </read_first>
  <behavior>
    - QuickExitButton renders fixed at top-right with z-index 9999
    - QuickExitButton click calls: history.replaceState(null, '', '/'), sessionStorage.clear(), window.location.replace('https://weather.naver.com')
    - QuickExitButton has aria-label "사이트 즉시 나가기"
    - QuickExitButton has min 56px touch target
    - EmergencyContacts renders: "여성긴급전화 1366" (tel:1366) + "경찰 112" (tel:112)
    - IncognitoGuidance renders on first visit only (sessionStorage flag)
    - IncognitoGuidance shows: "안전을 위해 시크릿(비공개) 모드 사용을 권장합니다"
  </behavior>
  <action>
1. `src/components/__tests__/QuickExitButton.test.tsx` — RED 테스트:
```tsx
import { render, screen, fireEvent } from '@testing-library/react';
import { QuickExitButton } from '../QuickExitButton';

describe('QuickExitButton', () => {
  const mockReplaceState = vi.fn();
  const mockReplace = vi.fn();

  beforeEach(() => {
    vi.stubGlobal('history', { ...history, replaceState: mockReplaceState });
    Object.defineProperty(window, 'location', {
      value: { replace: mockReplace },
      writable: true,
    });
    vi.spyOn(Storage.prototype, 'clear');
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('renders with accessible label', () => {
    render(<QuickExitButton />);
    expect(screen.getByLabelText('사이트 즉시 나가기')).toBeInTheDocument();
  });

  it('calls replaceState, clears storage, and redirects on click', () => {
    render(<QuickExitButton />);
    fireEvent.click(screen.getByLabelText('사이트 즉시 나가기'));
    expect(mockReplaceState).toHaveBeenCalledWith(null, '', '/');
    expect(sessionStorage.clear).toHaveBeenCalled();
    expect(mockReplace).toHaveBeenCalledWith('https://weather.naver.com');
  });
});
```

2. `src/components/__tests__/EmergencyContacts.test.tsx` — RED 테스트:
```tsx
import { render, screen } from '@testing-library/react';
import { EmergencyContacts } from '../EmergencyContacts';

describe('EmergencyContacts', () => {
  it('renders 1366 emergency number', () => {
    render(<EmergencyContacts />);
    expect(screen.getByText(/1366/)).toBeInTheDocument();
  });

  it('renders 112 police number', () => {
    render(<EmergencyContacts />);
    expect(screen.getByText(/112/)).toBeInTheDocument();
  });

  it('has tel: links', () => {
    render(<EmergencyContacts />);
    const links = screen.getAllByRole('link');
    const hrefs = links.map(l => l.getAttribute('href'));
    expect(hrefs).toContain('tel:1366');
    expect(hrefs).toContain('tel:112');
  });
});
```

3. `src/components/QuickExitButton.tsx` — GREEN 구현 (SAFE-01):
```tsx
'use client';

export function QuickExitButton() {
  const handleExit = () => {
    // 브라우저 히스토리에서 현재 페이지 제거
    window.history.replaceState(null, '', '/');
    // 세션 저장소 초기화
    sessionStorage.clear();
    // 중립 사이트로 이동 (네이버 날씨 — 한국 사용자 친숙)
    window.location.replace('https://weather.naver.com');
  };

  return (
    <button
      onClick={handleExit}
      aria-label="사이트 즉시 나가기"
      className="fixed top-4 right-4 z-[9999] min-h-[56px] min-w-[56px] bg-gray-600 hover:bg-gray-700 text-white font-bold rounded-lg px-4 py-2 shadow-lg transition-colors"
      style={{ paddingRight: 'max(1rem, env(safe-area-inset-right))' }}
    >
      나가기 ✕
    </button>
  );
}
```

4. `src/components/EmergencyContacts.tsx` — GREEN 구현 (SAFE-03):
```tsx
export function EmergencyContacts() {
  return (
    <div className="w-full bg-gray-50 border-t border-gray-200 px-4 py-3">
      <div className="max-w-4xl mx-auto flex flex-wrap items-center justify-center gap-4 text-sm text-gray-600">
        <span>긴급 도움이 필요하신가요?</span>
        <a
          href="tel:1366"
          className="inline-flex items-center min-h-[48px] px-3 py-1 text-red-700 font-semibold hover:underline"
        >
          여성긴급전화 1366
        </a>
        <a
          href="tel:112"
          className="inline-flex items-center min-h-[48px] px-3 py-1 text-red-700 font-semibold hover:underline"
        >
          경찰 112
        </a>
      </div>
    </div>
  );
}
```

5. `src/components/IncognitoGuidance.tsx` — (SAFE-05):
```tsx
'use client';

import { useState, useEffect } from 'react';

export function IncognitoGuidance() {
  const [show, setShow] = useState(false);

  useEffect(() => {
    // 세션 내 첫 방문인지 확인 (sessionStorage는 탭 닫으면 삭제됨)
    const seen = sessionStorage.getItem('incognito_guidance_seen');
    if (!seen) {
      setShow(true);
      sessionStorage.setItem('incognito_guidance_seen', 'true');
    }
  }, []);

  if (!show) return null;

  return (
    <div className="fixed bottom-4 left-4 right-4 z-[9998] max-w-md mx-auto bg-white border border-gray-300 rounded-lg shadow-lg p-4">
      <div className="flex justify-between items-start">
        <div>
          <p className="text-sm font-semibold text-gray-900 mb-1">
            안전을 위해 시크릿(비공개) 모드 사용을 권장합니다
          </p>
          <p className="text-xs text-gray-600">
            시크릿 모드에서는 방문 기록이 브라우저에 저장되지 않습니다.
            Chrome: Ctrl+Shift+N / Safari: Cmd+Shift+N
          </p>
        </div>
        <button
          onClick={() => setShow(false)}
          aria-label="안내 닫기"
          className="min-h-[48px] min-w-[48px] flex items-center justify-center text-gray-400 hover:text-gray-600"
        >
          ✕
        </button>
      </div>
    </div>
  );
}
```

**주의**: IncognitoGuidance는 sessionStorage를 사용하지만 이것은 사용자 법률 데이터가 아닌 UI 표시 플래그일 뿐이다. QuickExitButton이 sessionStorage.clear()를 호출하면 이 플래그도 함께 삭제된다.
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && npx vitest run src/components/__tests__/QuickExitButton.test.tsx src/components/__tests__/EmergencyContacts.test.tsx --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/components/QuickExitButton.tsx contains "history.replaceState(null, '', '/')"
    - src/components/QuickExitButton.tsx contains "sessionStorage.clear()"
    - src/components/QuickExitButton.tsx contains "window.location.replace('https://weather.naver.com')"
    - src/components/QuickExitButton.tsx contains "z-[9999]"
    - src/components/QuickExitButton.tsx contains "min-h-[56px]"
    - src/components/QuickExitButton.tsx contains "aria-label=\"사이트 즉시 나가기\""
    - src/components/EmergencyContacts.tsx contains "tel:1366"
    - src/components/EmergencyContacts.tsx contains "tel:112"
    - src/components/EmergencyContacts.tsx contains "min-h-[48px]"
    - src/components/IncognitoGuidance.tsx contains "시크릿(비공개) 모드"
    - src/components/IncognitoGuidance.tsx contains "'use client'"
    - QuickExitButton and EmergencyContacts tests exit 0
  </acceptance_criteria>
  <done>긴급 탈출 버튼이 history.replaceState + sessionStorage.clear + 중립 사이트 이동을 수행하고, 긴급연락처와 시크릿 모드 안내가 구현됨</done>
</task>

<task type="auto">
  <name>Task 3: locale 레이아웃에 모든 Safety/Compliance 컴포넌트 통합</name>
  <files>src/app/[locale]/layout.tsx</files>
  <read_first>
    - src/app/[locale]/layout.tsx (Plan 01 Task 2에서 생성한 현재 상태)
    - src/components/DisclaimerBanner.tsx (Task 1에서 생성)
    - src/components/QuickExitButton.tsx (Task 2에서 생성)
    - src/components/EmergencyContacts.tsx (Task 2에서 생성)
    - src/components/IncognitoGuidance.tsx (Task 2에서 생성)
    - src/components/PrivacyNotice.tsx (Task 1에서 생성)
  </read_first>
  <action>
`src/app/[locale]/layout.tsx`를 업데이트하여 모든 Safety/Compliance 컴포넌트를 배치한다. 레이아웃 구조:

```tsx
import type { Metadata } from 'next';
import { AuthProvider } from '@/components/AuthProvider';
import { DisclaimerBanner } from '@/components/DisclaimerBanner';
import { QuickExitButton } from '@/components/QuickExitButton';
import { EmergencyContacts } from '@/components/EmergencyContacts';
import { IncognitoGuidance } from '@/components/IncognitoGuidance';
import { PrivacyNotice } from '@/components/PrivacyNotice';

export const metadata: Metadata = {
  title: '생활 법률 안내',
  description: '상황에 맞는 법률 정보를 정리해 드립니다.',
  robots: { index: false, follow: false },
};

export default function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: { locale: string };
}) {
  return (
    <AuthProvider>
      {/* SAFE-01: 긴급 탈출 — 항상 최상위, z-9999 */}
      <QuickExitButton />

      {/* COMPL-01: 면책 고지 — 모든 페이지 상단 고정, 조건부 렌더링 금지 */}
      <DisclaimerBanner />

      {/* 메인 콘텐츠 */}
      <main className="min-h-screen bg-warm-gray font-[Pretendard] pt-2">
        {children}
      </main>

      {/* PRIV-04: AI 학습 미사용 고지 */}
      <PrivacyNotice />

      {/* SAFE-03: 긴급연락처 — 페이지 하단 고정 */}
      <EmergencyContacts />

      {/* SAFE-05: 시크릿 모드 안내 — 첫 방문 시만 */}
      <IncognitoGuidance />
    </AuthProvider>
  );
}
```

배치 규칙:
- DisclaimerBanner: `children` 위에 항상 렌더링. 조건부 렌더링 절대 금지 (COMPL-01, Pitfall 3).
- QuickExitButton: fixed position이므로 DOM 위치와 무관하나 최상위에 배치하여 의도 명확화.
- EmergencyContacts: 페이지 하단에 항상 표시.
- IncognitoGuidance: 클라이언트 사이드에서 sessionStorage 체크 후 조건부 표시.
- PrivacyNotice: EmergencyContacts 위에 배치.

SAFE-02 확인: metadata.title이 "생활 법률 안내"이며 "이혼"이 포함되지 않음.
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && grep "DisclaimerBanner" src/app/\\[locale\\]/layout.tsx && grep "QuickExitButton" src/app/\\[locale\\]/layout.tsx && grep "EmergencyContacts" src/app/\\[locale\\]/layout.tsx && grep "IncognitoGuidance" src/app/\\[locale\\]/layout.tsx && grep "PrivacyNotice" src/app/\\[locale\\]/layout.tsx && ! grep "이혼" src/app/\\[locale\\]/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/layout.tsx contains "import { DisclaimerBanner }"
    - src/app/[locale]/layout.tsx contains "import { QuickExitButton }"
    - src/app/[locale]/layout.tsx contains "import { EmergencyContacts }"
    - src/app/[locale]/layout.tsx contains "import { IncognitoGuidance }"
    - src/app/[locale]/layout.tsx contains "import { PrivacyNotice }"
    - src/app/[locale]/layout.tsx contains "<DisclaimerBanner />"
    - src/app/[locale]/layout.tsx contains "<QuickExitButton />"
    - src/app/[locale]/layout.tsx contains "<EmergencyContacts />"
    - src/app/[locale]/layout.tsx does NOT contain "이혼" (SAFE-02)
    - DisclaimerBanner appears BEFORE {children} in JSX order
    - EmergencyContacts appears AFTER {children} in JSX order
  </acceptance_criteria>
  <done>locale 레이아웃에 모든 Safety/Compliance 컴포넌트가 올바른 순서로 배치됨. 면책고지는 항상 상단, 긴급탈출은 fixed, 긴급연락처는 하단.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client UI | 사용자가 직접 상호작용하는 브라우저 내 컴포넌트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information Disclosure | QuickExitButton | mitigate | history.replaceState로 브라우저 히스토리에서 현재 페이지 제거. sessionStorage.clear()로 세션 데이터 삭제. window.location.replace로 현재 URL 교체(뒤로가기 불가). |
| T-01-09 | Elevation of Privilege | IncognitoGuidance sessionStorage | accept | sessionStorage에 "incognito_guidance_seen" 플래그만 저장. 법률 데이터 아님. QuickExitButton이 clear()로 함께 삭제. |
| T-01-10 | Information Disclosure | metadata SAFE-02 | mitigate | 페이지 타이틀/description에 "이혼" 포함 금지. robots noindex/nofollow로 검색엔진 색인 차단. |
</threat_model>

<verification>
1. `npx vitest run src/components/__tests__/ --reporter=verbose` — 모든 컴포넌트 테스트 통과
2. `grep -r "이혼" src/app/` — 0 결과 (SAFE-02 위반 없음)
3. `grep "DisclaimerBanner\|QuickExitButton\|EmergencyContacts" src/app/[locale]/layout.tsx` — 3개 모두 존재
</verification>

<success_criteria>
- 6개 컴포넌트 (DisclaimerBanner, LawyerCTA, QuickExitButton, EmergencyContacts, IncognitoGuidance, PrivacyNotice) 모두 구현
- locale 레이아웃에 올바른 순서로 통합
- QuickExitButton이 history.replaceState + sessionStorage.clear + neutral redirect 수행
- 페이지 메타데이터에 "이혼" 미포함
- 테스트 5개 이상 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
