---
phase: 01-foundation
plan: 04
type: tdd
wave: 2
depends_on:
  - 01
files_modified:
  - src/lib/claudeClient.ts
  - src/lib/complianceFilter.ts
  - src/lib/__tests__/complianceFilter.test.ts
  - src/lib/__tests__/claudeClient.test.ts
autonomous: true
requirements:
  - COMPL-03
  - COMPL-04

must_haves:
  truths:
    - "Claude 시스템 프롬프트에 '법적 결론 금지' 규칙이 포함된다"
    - "시스템 프롬프트에 금지 표현 목록(귀하의 경우, ~이 인정됩니다, 승소율 등)이 명시된다"
    - "후처리 필터가 '귀하의 경우' 패턴을 감지하면 해당 청크를 차단한다"
    - "후처리 필터가 '%가능성/확률' 패턴을 감지하면 해당 청크를 차단한다"
    - "후처리 필터가 금액 예측(만원/억) 패턴을 감지하면 해당 청크를 차단한다"
    - "필터를 통과한 안전한 텍스트는 그대로 반환된다"
  artifacts:
    - path: "src/lib/claudeClient.ts"
      provides: "Anthropic SDK 래퍼 + 시스템 프롬프트 가드레일"
      exports: ["claudeClient", "SYSTEM_PROMPT"]
      contains: "법적 결론을 내리지 마세요"
    - path: "src/lib/complianceFilter.ts"
      provides: "스트리밍 청크 후처리 필터"
      exports: ["applyFilter", "FORBIDDEN_PATTERNS"]
      contains: "귀하의.*경우"
    - path: "src/lib/__tests__/complianceFilter.test.ts"
      provides: "필터 동작 테스트"
      min_lines: 50
  key_links:
    - from: "src/lib/complianceFilter.ts"
      to: "streaming chunks"
      via: "applyFilter called on each chunk before enqueue"
      pattern: "applyFilter"
    - from: "src/lib/claudeClient.ts"
      to: "@anthropic-ai/sdk"
      via: "new Anthropic() + messages.stream()"
      pattern: "Anthropic"
---

<objective>
Claude API 래퍼와 변호사법 준수 후처리 필터를 TDD로 구현한다. 시스템 프롬프트 가드레일(1차 방어)과 출력 필터(2차 방어) 이중 방어 체계.

Purpose: 변호사법 109조 위반 방지는 서비스의 법적 생존 요건. 시스템 프롬프트만으로는 불충분하며 후처리 필터가 반드시 필요.
Output: `claudeClient.ts` + `complianceFilter.ts` — 이중 가드레일이 적용된 Claude 스트리밍 인터페이스
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/types/analysis.ts
</context>

<feature>
  <name>변호사법 준수 이중 가드레일 (COMPL-03 + COMPL-04)</name>
  <files>src/lib/claudeClient.ts, src/lib/complianceFilter.ts, src/lib/__tests__/complianceFilter.test.ts, src/lib/__tests__/claudeClient.test.ts</files>
  <behavior>
    ## complianceFilter
    - applyFilter("일반적인 쟁점입니다") → "일반적인 쟁점입니다" (통과)
    - applyFilter("귀하의 경우 위자료가...") → null (차단)
    - applyFilter("귀하에게 적용되는 법률은...") → null (차단)
    - applyFilter("승소할 확률은 70%입니다") → null (차단)
    - applyFilter("위자료 5000만원 수준") → null (차단)
    - applyFilter("재산분할 2억원") → null (차단)
    - applyFilter("~이 인정됩니다") → null (차단)
    - applyFilter("유리합니다") → "유리합니다" (단독 사용은 통과 — 법적 컨텍스트 결합 시만 차단)
    - 차단 시 console.warn 호출됨

    ## claudeClient
    - SYSTEM_PROMPT에 "법적 결론을 내리지 마세요" 포함
    - SYSTEM_PROMPT에 금지 표현 목록 포함
    - SYSTEM_PROMPT에 "법제처 API 검색 결과에서만 인용" 포함
    - stream() 메서드가 AsyncIterable<string> 반환
    - stream() 내부에서 Anthropic messages.stream() 호출
  </behavior>
  <implementation>
1. `src/lib/__tests__/complianceFilter.test.ts` — RED 테스트 먼저:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { applyFilter, FORBIDDEN_PATTERNS } from '../complianceFilter';

describe('complianceFilter', () => {
  it('passes safe text through unchanged', () => {
    expect(applyFilter('이혼 시 일반적으로 검토되는 쟁점입니다.')).toBe('이혼 시 일반적으로 검토되는 쟁점입니다.');
  });

  it('blocks "귀하의 경우" pattern', () => {
    const warnSpy = vi.spyOn(console, 'warn').mockImplementation(() => {});
    expect(applyFilter('귀하의 경우 위자료 청구가 가능합니다.')).toBeNull();
    expect(warnSpy).toHaveBeenCalled();
    warnSpy.mockRestore();
  });

  it('blocks "귀하에게 적용" pattern', () => {
    expect(applyFilter('귀하에게 적용되는 법률은 민법 제840조입니다.')).toBeNull();
  });

  it('blocks percentage prediction pattern', () => {
    expect(applyFilter('승소할 확률은 70%입니다.')).toBeNull();
  });

  it('blocks monetary amount with legal context', () => {
    expect(applyFilter('위자료 5000만원 수준으로 예상됩니다.')).toBeNull();
  });

  it('blocks "재산분할" + amount pattern', () => {
    expect(applyFilter('재산분할 금액은 2억원 정도입니다.')).toBeNull();
  });

  it('blocks "~이 인정됩니다" legal conclusion', () => {
    expect(applyFilter('이혼 사유가 인정됩니다.')).toBeNull();
  });

  it('blocks "~가 유리합니다" legal advice', () => {
    expect(applyFilter('귀하에게 재판이혼이 유리합니다.')).toBeNull();
  });

  it('has at least 5 forbidden patterns', () => {
    expect(FORBIDDEN_PATTERNS.length).toBeGreaterThanOrEqual(5);
  });
});
```

2. `src/lib/complianceFilter.ts` — GREEN 구현 (COMPL-03 + COMPL-04):
```typescript
export const FORBIDDEN_PATTERNS: RegExp[] = [
  /귀하의\s*경우/,
  /귀하에게\s*(적용|해당|유리)/,
  /인정됩니다/,
  /가능성이\s*(높|낮)/,
  /\d+%\s*(가능성|확률|승소)/,
  /(?:위자료|재산분할).*\d+(?:만원|억)/,
  /(?:받으실|받을)\s*수\s*있/,
  /판결.{0,10}예상/,
];

export function applyFilter(text: string): string | null {
  for (const pattern of FORBIDDEN_PATTERNS) {
    if (pattern.test(text)) {
      console.warn('[ComplianceFilter] Blocked chunk:', text.slice(0, 100));
      return null;
    }
  }
  return text;
}
```

3. `src/lib/__tests__/claudeClient.test.ts` — 시스템 프롬프트 검증:
```typescript
import { describe, it, expect } from 'vitest';
import { SYSTEM_PROMPT } from '../claudeClient';

describe('claudeClient SYSTEM_PROMPT', () => {
  it('contains no legal conclusions rule', () => {
    expect(SYSTEM_PROMPT).toContain('법적 결론을 내리지 마세요');
  });

  it('contains forbidden expression examples', () => {
    expect(SYSTEM_PROMPT).toContain('귀하의 경우');
  });

  it('contains law API sourcing rule', () => {
    expect(SYSTEM_PROMPT).toContain('법제처 API 검색 결과에서만 인용');
  });

  it('contains response format directive', () => {
    expect(SYSTEM_PROMPT).toContain('쟁점');
  });

  it('contains trailing disclaimer directive', () => {
    expect(SYSTEM_PROMPT).toContain('법률 자문이 아닙니다');
  });
});
```

4. `src/lib/claudeClient.ts` — GREEN 구현:
```typescript
import Anthropic from '@anthropic-ai/sdk';

export const SYSTEM_PROMPT = `
당신은 법률 정보를 제공하는 도구입니다. 아래 규칙을 절대 위반하지 마세요:

1. 법적 결론을 내리지 마세요. 금지 표현: "~이 인정됩니다", "~가 유리합니다", "귀하의 경우", "~할 가능성이 높습니다"
2. 재산분할/위자료 금액, 양육권 결과, 승소율을 예측하지 마세요.
3. 모든 법령과 판례는 법제처 API 검색 결과에서만 인용하세요. 번호나 내용을 생성하지 마세요.
4. 응답 형식: 반드시 "이혼 시 일반적으로 검토되는 쟁점" 형태로만 제시하세요.
5. 매 응답 끝에: "이 정보는 법률 자문이 아닙니다. 변호사 상담을 받으시기 바랍니다."
`.trim();

const anthropic = new Anthropic({
  apiKey: process.env.ANTHROPIC_API_KEY,
});

export const claudeClient = {
  async *stream(userPrompt: string): AsyncGenerator<string> {
    const stream = anthropic.messages.stream({
      model: 'claude-sonnet-4-6',
      max_tokens: 4096,
      system: SYSTEM_PROMPT,
      messages: [{ role: 'user', content: userPrompt }],
    });

    for await (const event of stream) {
      if (event.type === 'content_block_delta' && event.delta.type === 'text_delta') {
        yield event.delta.text;
      }
    }
  },
};
```
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Claude API | 사용자 입력이 Claude에 전송됨. 시스템 프롬프트로 출력 제한. |
| Claude Output -> Client | Claude 스트리밍 출력이 클라이언트로 전달됨. 후처리 필터 통과 필수. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | SYSTEM_PROMPT | mitigate | 시스템 프롬프트는 서버 코드에 하드코딩. 클라이언트에서 수정 불가. 사용자 입력은 user role로만 전달. |
| T-01-12 | Repudiation | complianceFilter | mitigate | 차단된 청크는 console.warn으로 로깅. 프로덕션에서 모니터링 가능. |
| T-01-13 | Information Disclosure | claudeClient | mitigate | ANTHROPIC_API_KEY는 서버 전용 환경변수. SYSTEM_PROMPT는 서버에서만 사용. 클라이언트에 노출 안 됨. |
| T-01-14 | Tampering | prompt injection | mitigate | 시스템 프롬프트 1차 방어 + complianceFilter 2차 방어. 시스템 프롬프트 우회해도 후처리 필터에서 금지 패턴 차단. |
</threat_model>

<verification>
1. `npx vitest run src/lib/__tests__/complianceFilter.test.ts --reporter=verbose` — 모든 필터 테스트 통과
2. `npx vitest run src/lib/__tests__/claudeClient.test.ts --reporter=verbose` — 시스템 프롬프트 검증 통과
3. SYSTEM_PROMPT에 5개 규칙 모두 포함 확인
</verification>

<success_criteria>
- complianceFilter가 최소 5개 금지 패턴을 검출
- SYSTEM_PROMPT에 법적 결론 금지 + 금지 표현 + API 출처 제한 + 응답 형식 + 면책 고지 5개 규칙 포함
- claudeClient.stream()이 AsyncGenerator<string> 반환
- 테스트 14개 이상 GREEN (complianceFilter 9 + claudeClient 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
