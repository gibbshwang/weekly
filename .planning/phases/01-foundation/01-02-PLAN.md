---
phase: 01-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/koreanLawClient.ts
  - src/lib/__tests__/koreanLawClient.test.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "법제처 API에 법령 키워드를 검색하면 Statute[] 배열이 반환된다"
    - "법제처 API에 판례 키워드를 검색하면 Precedent[] 배열이 반환된다"
    - "API 타임아웃 시 빈 배열이 반환된다 (에러 throw 아님)"
    - "XML 응답의 HTML 태그가 제거된 깨끗한 텍스트가 반환된다"
    - "500ms rate-limit 가드가 연속 호출을 제어한다"
  artifacts:
    - path: "src/lib/koreanLawClient.ts"
      provides: "법제처 API 래퍼 (XML→JSON)"
      exports: ["searchStatutes", "searchPrecedents", "getStatuteDetail"]
    - path: "src/lib/__tests__/koreanLawClient.test.ts"
      provides: "TDD 테스트 (mock XML 응답)"
      min_lines: 80
  key_links:
    - from: "src/lib/koreanLawClient.ts"
      to: "https://www.law.go.kr/DRF/lawSearch.do"
      via: "fetch with OC key from env"
      pattern: "law\\.go\\.kr"
---

<objective>
법제처 Open API 래퍼를 TDD로 구현한다. XML 응답을 파싱하여 Statute[]/Precedent[] 타입으로 변환하고, rate-limit 가드와 타임아웃 처리를 포함한다.

Purpose: 전체 분석 파이프라인의 데이터 소스. 법제처 API 없이는 법령/판례 검색이 불가능.
Output: `src/lib/koreanLawClient.ts` — 검증된 API 래퍼 + 테스트 스위트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/types/law.ts
</context>

<feature>
  <name>법제처 Open API 래퍼 (XML→JSON, INFRA-01)</name>
  <files>src/lib/koreanLawClient.ts, src/lib/__tests__/koreanLawClient.test.ts</files>
  <behavior>
    - searchStatutes("이혼") → Statute[] (name, mst, category, effectiveDate 필드 존재)
    - searchPrecedents(["이혼", "재산분할"]) → Precedent[] (caseNumber, date, summary, ruling 필드 존재)
    - getStatuteDetail("법령MST값") → StatuteArticle[] (articleNumber, articleTitle, articleContent 필드 존재)
    - API 타임아웃(3초 초과) → 빈 배열 반환, 에러 throw 안 함
    - API 500 에러 → 빈 배열 반환, console.error 로깅
    - HTML 태그 포함 텍스트(<br/>, <p> 등) → 태그 제거된 순수 텍스트 반환
    - 연속 2회 호출 → 두 번째 호출이 500ms 이상 간격으로 실행됨
    - OC 키가 없으면 → 빈 배열 반환, 에러 메시지 로깅
  </behavior>
  <implementation>
1. `src/lib/__tests__/koreanLawClient.test.ts` — RED 테스트 먼저 작성:
   - `vi.stubGlobal('fetch', ...)` 또는 `vi.fn()`으로 fetch를 모킹
   - 법령 검색 성공 케이스: mock XML 응답 → Statute[] 매핑 검증
   - 판례 검색 성공 케이스: mock XML 응답 → Precedent[] 매핑 검증
   - 타임아웃 케이스: fetch가 AbortError throw → 빈 배열 반환 확인
   - HTML strip 케이스: `<br/>판시사항<p>내용</p>` → "판시사항 내용" 확인
   - Rate limit 케이스: 연속 호출 시 두 번째 호출 전 delay 확인

2. `src/lib/koreanLawClient.ts` — GREEN 구현:
   - `fast-xml-parser`의 `XMLParser` 사용 (`import { XMLParser } from 'fast-xml-parser'`)
   - `process.env.KOREAN_LAW_OC_KEY`에서 OC 키 읽기 (없으면 빈 배열 early return)
   - BASE_URL: `'https://www.law.go.kr/DRF'` (HTTPS 먼저 시도, 실패 시 HTTP fallback은 별도 이슈)
   - `stripHtml(text: string): string` — `text.replace(/<[^>]*>/g, ' ').replace(/\s+/g, ' ').trim()`
   - `rateLimitedFetch(url: string): Promise<string>` — 마지막 호출로부터 500ms 미만이면 대기
   - `searchStatutes(query: string): Promise<Statute[]>` — `/lawSearch.do?OC=...&target=law&type=XML&query=...`
   - `searchPrecedents(keywords: string[]): Promise<Precedent[]>` — `/lawSearch.do?OC=...&target=prec&type=XML&query=...&display=5`
   - `getStatuteDetail(mst: string): Promise<StatuteArticle[]>` — `/lawService.do?OC=...&MST=...&type=XML`
   - 모든 fetch에 `signal: AbortSignal.timeout(3000)` 적용
   - try/catch로 감싸서 에러 시 빈 배열 반환 + console.error 로깅
   - XML 필드명은 RESEARCH.md의 ASSUMED 값 사용 (법령명한글, 법령MST, 사건번호, 판시사항, 판결요지). 실제 필드명이 다르면 테스트에서 즉시 잡힘.

Mock XML 응답 예시 (테스트에서 사용):
```xml
<!-- 법령 검색 -->
<LawSearch>
  <law>
    <법령명한글>민법</법령명한글>
    <법령MST>001234</법령MST>
    <법령구분명>법률</법령구분명>
    <시행일자>20240101</시행일자>
  </law>
</LawSearch>

<!-- 판례 검색 -->
<PrecSearch>
  <prec>
    <사건번호>2023다12345</사건번호>
    <선고일자>20231215</선고일자>
    <판시사항><![CDATA[이혼 시 <br/>재산분할 기준]]></판시사항>
    <판결요지><![CDATA[<p>재산분할 비율은...</p>]]></판결요지>
  </prec>
</PrecSearch>
```
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server -> Law API | Route Handler에서 법제처 Open API로의 outbound HTTP 요청 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | koreanLawClient.ts | mitigate | OC 키는 process.env.KOREAN_LAW_OC_KEY (서버 전용). NEXT_PUBLIC_ prefix 금지. 클라이언트 코드에서 직접 호출 불가. |
| T-01-06 | Denial of Service | rateLimitedFetch | mitigate | 500ms rate-limit 가드로 법제처 API 과다 호출 방지. AbortSignal.timeout(3000)으로 hung request 방지. |
| T-01-07 | Tampering | XML response | accept | 법제처는 정부 공개 API. 응답 위변조 위험 낮음. HTTPS 사용으로 전송 중 변조 방지. |
</threat_model>

<verification>
1. `npx vitest run src/lib/__tests__/koreanLawClient.test.ts` — 모든 테스트 통과
2. 테스트가 실제 API 호출 없이 mock으로만 동작 (네트워크 의존성 없음)
3. `grep -c "test\|it(" src/lib/__tests__/koreanLawClient.test.ts` — 최소 6개 테스트 케이스
</verification>

<success_criteria>
- koreanLawClient.ts가 searchStatutes, searchPrecedents, getStatuteDetail을 export
- 모든 함수가 에러 시 빈 배열 반환 (throw 안 함)
- HTML strip 유틸이 <br/>, <p> 등을 제거
- Rate limit 가드가 500ms 간격 보장
- 테스트 6개 이상 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
