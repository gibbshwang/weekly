---
phase: 01-foundation
plan: 05
type: execute
wave: 2
depends_on:
  - 01
  - 02
  - 04
files_modified:
  - src/lib/pipelineOrchestrator.ts
  - src/app/api/analyze/route.ts
  - src/lib/__tests__/pipelineOrchestrator.test.ts
autonomous: false
requirements:
  - INFRA-02

must_haves:
  truths:
    - "POST /api/analyze에 UserSituation JSON을 전송하면 스트리밍 응답이 반환된다"
    - "스트리밍 응답의 Content-Type은 text/plain; charset=utf-8이다"
    - "법제처 API 검색 결과가 Claude 프롬프트에 포함된다"
    - "Claude 스트리밍 청크가 complianceFilter를 거친 후에만 클라이언트에 전달된다"
    - "runtime = 'nodejs'와 maxDuration = 60이 설정되어 있다"
  artifacts:
    - path: "src/app/api/analyze/route.ts"
      provides: "POST 분석 엔드포인트 (스트리밍)"
      exports: ["POST"]
      contains: "runtime = 'nodejs'"
    - path: "src/lib/pipelineOrchestrator.ts"
      provides: "법제처→Claude 파이프라인 조합"
      exports: ["orchestrateAnalysis"]
    - path: "src/lib/__tests__/pipelineOrchestrator.test.ts"
      provides: "파이프라인 오케스트레이션 테스트"
      min_lines: 40
  key_links:
    - from: "src/app/api/analyze/route.ts"
      to: "src/lib/pipelineOrchestrator.ts"
      via: "import { orchestrateAnalysis }"
      pattern: "orchestrateAnalysis"
    - from: "src/lib/pipelineOrchestrator.ts"
      to: "src/lib/koreanLawClient.ts"
      via: "import { searchStatutes, searchPrecedents }"
      pattern: "searchStatutes"
    - from: "src/lib/pipelineOrchestrator.ts"
      to: "src/lib/claudeClient.ts"
      via: "import { claudeClient }"
      pattern: "claudeClient"
    - from: "src/lib/pipelineOrchestrator.ts"
      to: "src/lib/complianceFilter.ts"
      via: "import { applyFilter }"
      pattern: "applyFilter"
---

<objective>
법제처 API -> Claude 스트리밍 -> 후처리 필터를 연결하는 분석 파이프라인과 Route Handler를 구현한다.

Purpose: Phase 1의 핵심 성공 기준 — "POST /api/analyze에 상황 데이터를 전송하면 법제처 API 검색 결과를 포함한 Claude 스트리밍 응답이 반환된다". 이 엔드포인트가 없으면 Phase 2 위저드 UI가 연결할 대상이 없다.
Output: `POST /api/analyze` Route Handler + `pipelineOrchestrator.ts` — 완전한 분석 스트리밍 파이프라인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@src/types/law.ts
@src/types/analysis.ts
@src/lib/koreanLawClient.ts
@src/lib/claudeClient.ts
@src/lib/complianceFilter.ts
</context>

<interfaces>
<!-- Plan 01, 02, 04에서 생성된 타입/exports. Executor는 이 인터페이스를 직접 사용. -->

From src/types/law.ts:
```typescript
export interface Statute { name: string; mst: string; category: string; effectiveDate: string; }
export interface Precedent { caseNumber: string; date: string; summary: string; ruling: string; precSeq?: string; }
```

From src/types/analysis.ts:
```typescript
export interface UserSituation { marriageDuration: string; hasChildren: boolean; childrenInfo?: string; assetOverview?: string; divorceReason: string; additionalContext?: string; }
```

From src/lib/koreanLawClient.ts:
```typescript
export async function searchStatutes(query: string): Promise<Statute[]>;
export async function searchPrecedents(keywords: string[]): Promise<Precedent[]>;
```

From src/lib/claudeClient.ts:
```typescript
export const SYSTEM_PROMPT: string;
export const claudeClient: { stream(userPrompt: string): AsyncGenerator<string> };
```

From src/lib/complianceFilter.ts:
```typescript
export function applyFilter(text: string): string | null;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pipelineOrchestrator — 법제처→Claude→필터 파이프라인 조합</name>
  <files>src/lib/pipelineOrchestrator.ts, src/lib/__tests__/pipelineOrchestrator.test.ts</files>
  <read_first>
    - src/lib/koreanLawClient.ts (Plan 02에서 생성 — searchStatutes, searchPrecedents API)
    - src/lib/claudeClient.ts (Plan 04에서 생성 — stream, SYSTEM_PROMPT API)
    - src/lib/complianceFilter.ts (Plan 04에서 생성 — applyFilter API)
    - src/types/analysis.ts (Plan 01에서 생성 — UserSituation 타입)
    - src/types/law.ts (Plan 01에서 생성 — Statute, Precedent 타입)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: 즉시 스트림 반환)
  </read_first>
  <behavior>
    - orchestrateAnalysis(situation) returns ReadableStream
    - 파이프라인 순서: (1) 법제처 법령 검색 + 판례 검색 병렬 → (2) 결과를 Claude 프롬프트에 포함 → (3) Claude 스트리밍 → (4) 각 청크에 applyFilter 적용 → (5) 통과된 청크만 stream에 enqueue
    - 법제처 API 실패 시 graceful degradation: 빈 배열로 계속 진행 (에러 throw 안 함)
    - Claude 스트리밍 에러 시 에러 청크를 stream에 전달
  </behavior>
  <action>
1. `src/lib/__tests__/pipelineOrchestrator.test.ts` — RED 테스트:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';

// Mock dependencies
vi.mock('../koreanLawClient', () => ({
  searchStatutes: vi.fn(),
  searchPrecedents: vi.fn(),
}));
vi.mock('../claudeClient', () => ({
  claudeClient: {
    stream: vi.fn(),
  },
}));
vi.mock('../complianceFilter', () => ({
  applyFilter: vi.fn((text: string) => text), // 기본: 통과
}));

import { orchestrateAnalysis, buildUserPrompt } from '../pipelineOrchestrator';
import { searchStatutes, searchPrecedents } from '../koreanLawClient';
import { claudeClient } from '../claudeClient';
import { applyFilter } from '../complianceFilter';
import type { UserSituation } from '@/types/analysis';

const mockSituation: UserSituation = {
  marriageDuration: '10년',
  hasChildren: true,
  childrenInfo: '초등학생 2명',
  divorceReason: '성격 차이',
};

describe('buildUserPrompt', () => {
  it('includes user situation details', () => {
    const prompt = buildUserPrompt(mockSituation, [], []);
    expect(prompt).toContain('10년');
    expect(prompt).toContain('초등학생 2명');
    expect(prompt).toContain('성격 차이');
  });

  it('includes statute results when provided', () => {
    const statutes = [{ name: '민법', mst: '001', category: '법률', effectiveDate: '20240101' }];
    const prompt = buildUserPrompt(mockSituation, statutes, []);
    expect(prompt).toContain('민법');
  });

  it('includes precedent results when provided', () => {
    const precedents = [{ caseNumber: '2023다12345', date: '20231215', summary: '재산분할', ruling: '기각' }];
    const prompt = buildUserPrompt(mockSituation, [], precedents);
    expect(prompt).toContain('2023다12345');
  });
});

describe('orchestrateAnalysis', () => {
  beforeEach(() => {
    vi.mocked(searchStatutes).mockResolvedValue([]);
    vi.mocked(searchPrecedents).mockResolvedValue([]);
  });

  it('returns a ReadableStream', () => {
    async function* mockStream() { yield '테스트 텍스트'; }
    vi.mocked(claudeClient.stream).mockReturnValue(mockStream());

    const stream = orchestrateAnalysis(mockSituation);
    expect(stream).toBeInstanceOf(ReadableStream);
  });

  it('calls searchStatutes and searchPrecedents', async () => {
    async function* mockStream() { yield '완료'; }
    vi.mocked(claudeClient.stream).mockReturnValue(mockStream());

    const stream = orchestrateAnalysis(mockSituation);
    const reader = stream.getReader();
    while (true) {
      const { done } = await reader.read();
      if (done) break;
    }

    expect(searchStatutes).toHaveBeenCalled();
    expect(searchPrecedents).toHaveBeenCalled();
  });

  it('applies compliance filter to each chunk', async () => {
    async function* mockStream() { yield '청크1'; yield '청크2'; }
    vi.mocked(claudeClient.stream).mockReturnValue(mockStream());

    const stream = orchestrateAnalysis(mockSituation);
    const reader = stream.getReader();
    while (true) {
      const { done } = await reader.read();
      if (done) break;
    }

    expect(applyFilter).toHaveBeenCalledWith('청크1');
    expect(applyFilter).toHaveBeenCalledWith('청크2');
  });
});
```

2. `src/lib/pipelineOrchestrator.ts` — GREEN 구현:
```typescript
import { searchStatutes, searchPrecedents } from './koreanLawClient';
import { claudeClient } from './claudeClient';
import { applyFilter } from './complianceFilter';
import type { UserSituation } from '@/types/analysis';
import type { Statute, Precedent } from '@/types/law';

export function buildUserPrompt(
  situation: UserSituation,
  statutes: Statute[],
  precedents: Precedent[],
): string {
  const parts: string[] = [];

  parts.push('## 사용자 상황');
  parts.push(`- 결혼 기간: ${situation.marriageDuration}`);
  parts.push(`- 자녀: ${situation.hasChildren ? situation.childrenInfo || '있음' : '없음'}`);
  if (situation.assetOverview) parts.push(`- 재산 개요: ${situation.assetOverview}`);
  parts.push(`- 이혼 사유: ${situation.divorceReason}`);
  if (situation.additionalContext) parts.push(`- 추가 사항: ${situation.additionalContext}`);

  if (statutes.length > 0) {
    parts.push('\n## 법제처 API 검색 결과 — 관련 법령');
    statutes.forEach((s, i) => {
      parts.push(`${i + 1}. ${s.name} (${s.category}, 시행 ${s.effectiveDate})`);
    });
  }

  if (precedents.length > 0) {
    parts.push('\n## 법제처 API 검색 결과 — 관련 판례');
    precedents.forEach((p, i) => {
      parts.push(`${i + 1}. ${p.caseNumber} (${p.date})`);
      parts.push(`   판시사항: ${p.summary}`);
    });
  }

  parts.push('\n위 상황과 법제처 검색 결과를 바탕으로:');
  parts.push('1. 이혼 시 일반적으로 검토되는 쟁점 체크리스트');
  parts.push('2. 관련 법령 조문 요약 (위 검색 결과에서만 인용)');
  parts.push('3. 관련 판례 요약 (위 검색 결과에서만 인용)');
  parts.push('4. 변호사에게 물어볼 질문 목록');
  parts.push('을 정리해주세요.');

  return parts.join('\n');
}

function buildSearchKeywords(situation: UserSituation): string[] {
  const keywords = ['이혼'];
  if (situation.hasChildren) keywords.push('양육권', '양육비');
  if (situation.assetOverview) keywords.push('재산분할');
  if (situation.divorceReason) {
    if (/폭력|폭행|학대/.test(situation.divorceReason)) keywords.push('가정폭력');
    if (/외도|불륜|부정/.test(situation.divorceReason)) keywords.push('부정행위');
    if (/별거/.test(situation.divorceReason)) keywords.push('별거');
  }
  return keywords;
}

export function orchestrateAnalysis(situation: UserSituation): ReadableStream<Uint8Array> {
  const encoder = new TextEncoder();

  return new ReadableStream({
    async start(controller) {
      try {
        const keywords = buildSearchKeywords(situation);
        const [statutes, precedents] = await Promise.all([
          searchStatutes(keywords.join(' ')),
          searchPrecedents(keywords),
        ]);

        const userPrompt = buildUserPrompt(situation, statutes, precedents);

        const stream = claudeClient.stream(userPrompt);
        for await (const chunk of stream) {
          const filtered = applyFilter(chunk);
          if (filtered !== null) {
            controller.enqueue(encoder.encode(filtered));
          }
        }
      } catch (err) {
        const errorMessage = err instanceof Error ? err.message : 'Unknown error';
        controller.enqueue(encoder.encode(`\n\n[오류가 발생했습니다: ${errorMessage}]`));
      } finally {
        controller.close();
      }
    },
  });
}
```
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && npx vitest run src/lib/__tests__/pipelineOrchestrator.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/pipelineOrchestrator.ts contains "export function orchestrateAnalysis"
    - src/lib/pipelineOrchestrator.ts contains "export function buildUserPrompt"
    - src/lib/pipelineOrchestrator.ts contains "Promise.all" (법제처 병렬 호출)
    - src/lib/pipelineOrchestrator.ts contains "applyFilter(chunk)"
    - src/lib/pipelineOrchestrator.ts contains "new ReadableStream"
    - src/lib/pipelineOrchestrator.ts contains "controller.close()"
    - pipelineOrchestrator test exits 0
  </acceptance_criteria>
  <done>법제처 병렬 검색 → Claude 프롬프트 조합 → 스트리밍 + 후처리 필터 파이프라인이 mock 기반 테스트와 함께 구현됨</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/analyze Route Handler</name>
  <files>src/app/api/analyze/route.ts</files>
  <read_first>
    - src/lib/pipelineOrchestrator.ts (Task 1에서 생성 — orchestrateAnalysis API)
    - src/types/analysis.ts (Plan 01에서 생성 — UserSituation 타입)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: 즉시 스트림 반환, maxDuration 설정)
  </read_first>
  <action>
`src/app/api/analyze/route.ts` 생성:

```typescript
import { orchestrateAnalysis } from '@/lib/pipelineOrchestrator';
import type { UserSituation } from '@/types/analysis';

// INFRA-03: Node.js runtime 필수 — Anthropic SDK + fast-xml-parser + firebase-admin은 Node.js API 의존
export const runtime = 'nodejs';
// INFRA-02: Vercel Pro 최대 300s. 법제처(~1s) + Claude 스트리밍(5~15s) 고려하여 60s 설정
export const maxDuration = 60;

export async function POST(req: Request) {
  try {
    const body = await req.json();

    // 기본 입력 검증
    const situation: UserSituation = {
      marriageDuration: body.marriageDuration ?? '',
      hasChildren: Boolean(body.hasChildren),
      childrenInfo: body.childrenInfo,
      assetOverview: body.assetOverview,
      divorceReason: body.divorceReason ?? '',
      additionalContext: body.additionalContext,
    };

    if (!situation.divorceReason) {
      return new Response(
        JSON.stringify({ error: '이혼 사유는 필수 입력 항목입니다.' }),
        { status: 400, headers: { 'Content-Type': 'application/json' } },
      );
    }

    // 스트리밍 응답 즉시 반환 (Vercel 타임아웃 회피)
    const stream = orchestrateAnalysis(situation);

    return new Response(stream, {
      headers: {
        'Content-Type': 'text/plain; charset=utf-8',
        'Transfer-Encoding': 'chunked',
        'Cache-Control': 'no-cache, no-store',
        'X-Content-Type-Options': 'nosniff',
      },
    });
  } catch (err) {
    console.error('[/api/analyze] Error:', err);
    return new Response(
      JSON.stringify({ error: '분석 요청 처리 중 오류가 발생했습니다.' }),
      { status: 500, headers: { 'Content-Type': 'application/json' } },
    );
  }
}
```

주의사항:
- `runtime = 'nodejs'` 필수 (INFRA-03). Edge runtime은 Anthropic SDK 미지원.
- `maxDuration = 60` 필수 (INFRA-02). 없으면 Vercel 기본 10s 타임아웃에 걸림.
- Response를 즉시 반환하고 내부에서 비동기 처리. ReadableStream의 start()에서 법제처 + Claude 호출.
- PRIV-02: 이 엔드포인트는 Firestore에 아무것도 쓰지 않음. 메모리에서만 처리.
  </action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && grep "runtime = 'nodejs'" src/app/api/analyze/route.ts && grep "maxDuration = 60" src/app/api/analyze/route.ts && grep "orchestrateAnalysis" src/app/api/analyze/route.ts && grep "Content-Type.*text/plain" src/app/api/analyze/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/analyze/route.ts contains "export const runtime = 'nodejs'"
    - src/app/api/analyze/route.ts contains "export const maxDuration = 60"
    - src/app/api/analyze/route.ts contains "export async function POST"
    - src/app/api/analyze/route.ts contains "orchestrateAnalysis(situation)"
    - src/app/api/analyze/route.ts contains "'Content-Type': 'text/plain; charset=utf-8'"
    - src/app/api/analyze/route.ts contains "'Cache-Control': 'no-cache, no-store'"
    - src/app/api/analyze/route.ts contains "divorceReason" (필수 입력 검증)
    - src/app/api/analyze/route.ts does NOT contain "Firestore" or "adminDb" (PRIV-02)
  </acceptance_criteria>
  <done>POST /api/analyze가 UserSituation을 받아 법제처→Claude 스트리밍 응답을 반환. runtime=nodejs, maxDuration=60, 서버 저장 없음.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 1 전체 통합 검증</name>
  <files>N/A</files>
  <action>Phase 1에서 구현한 전체 파이프라인의 통합 동작을 사용자가 직접 확인한다. 아래 검증 절차를 수행.</action>
  <verify>
    <automated>cd "C:\Users\hhc20\.openclaw\workspace" && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <what-built>Phase 1 전체 파이프라인: Next.js 15 스캐폴딩 + Firebase 익명인증 + 법제처 API 래퍼 + Claude 스트리밍 + 후처리 필터 + Safety/Compliance UI + POST /api/analyze 엔드포인트</what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 http://localhost:3000 접속
    2. 브라우저 탭 타이틀이 "생활 법률 안내"인지 확인 (SAFE-02: "이혼" 미포함)
    3. 페이지 상단에 면책 고지 배너가 표시되는지 확인 (COMPL-01)
    4. 우상단에 "나가기" 버튼이 보이는지 확인 (SAFE-01)
    5. 페이지 하단에 긴급연락처(1366, 112)가 보이는지 확인 (SAFE-03)
    6. 첫 방문 시 시크릿 모드 안내가 표시되는지 확인 (SAFE-05)
    7. curl로 분석 엔드포인트 테스트 (ANTHROPIC_API_KEY가 .env.local에 있어야 실제 스트리밍 확인 가능):
       ```bash
       curl -X POST http://localhost:3000/api/analyze \
         -H "Content-Type: application/json" \
         -d '{"marriageDuration":"5년","hasChildren":true,"childrenInfo":"아이 1명","divorceReason":"성격 차이"}'
       ```
    8. `npx vitest run --reporter=verbose` 실행 — 모든 테스트 GREEN 확인
  </how-to-verify>
  <done>사용자가 UI 검증(면책고지, 탈출버튼, 긴급연락처, 시크릿 안내) + API 스트리밍 응답 확인 후 "approved" 입력</done>
  <resume-signal>"approved"를 입력하거나 발견된 이슈를 설명해 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> POST /api/analyze | 사용자 상황 데이터가 서버 Route Handler로 전송됨 |
| Server -> Law API | Route Handler에서 법제처 Open API 호출 |
| Server -> Claude API | Route Handler에서 Anthropic API 호출 (사용자 입력 포함) |
| Claude -> Client (via filter) | Claude 출력이 complianceFilter를 거쳐 클라이언트에 스트리밍 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Tampering | POST body | mitigate | UserSituation 타입으로 입력 정규화. divorceReason 필수 검증. 추후 Phase 2에서 Zod 스키마 검증 강화. |
| T-01-16 | Denial of Service | /api/analyze | mitigate | maxDuration=60으로 long-running request 방지. 법제처 API에 AbortSignal.timeout(3000) 설정. Vercel 자체 rate limiting 활용. |
| T-01-17 | Information Disclosure | streaming response | mitigate | complianceFilter가 법적 결론 패턴 차단. Cache-Control: no-cache, no-store로 응답 캐싱 방지. X-Content-Type-Options: nosniff 설정. |
| T-01-18 | Repudiation | analysis request | accept | Phase 1에서는 요청 로깅 미구현. 서버 자동 저장 없음(PRIV-02). 추후 모니터링 추가 가능. |
</threat_model>

<verification>
1. `npx vitest run --reporter=verbose` — 전체 테스트 스위트 GREEN
2. `npx tsc --noEmit` — TypeScript 에러 0개
3. `curl -X POST http://localhost:3000/api/analyze -H "Content-Type: application/json" -d '{"divorceReason":"테스트"}' ` — 스트리밍 응답 수신 (또는 API 키 없으면 에러 메시지)
4. `grep -r "이혼" src/app/` — 0 결과 (SAFE-02)
5. `grep -r "localStorage" src/` — 0 결과 또는 법률 데이터 아닌 UI 플래그만 (PRIV-02)
</verification>

<success_criteria>
- POST /api/analyze가 스트리밍 응답을 반환
- 법제처 API 검색 결과가 Claude 프롬프트에 포함됨
- 모든 Claude 출력이 complianceFilter를 통과
- runtime='nodejs', maxDuration=60 설정 확인
- 전체 테스트 스위트 GREEN
- 페이지 타이틀에 "이혼" 미포함
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
