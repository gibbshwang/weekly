---
phase: 03-polish-accessibility-pipa-completeness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/api/account/route.ts
  - src/app/api/account/__tests__/route.test.ts
  - src/components/DeleteDataButton.tsx
  - src/components/__tests__/DeleteDataButton.test.tsx
  - src/components/wizard/WizardContainer.tsx
  - src/components/wizard/PipaConsentDialog.tsx
  - src/components/wizard/__tests__/PipaConsentDialog.test.tsx
  - src/stores/wizardStore.ts
  - src/app/[locale]/layout.tsx
autonomous: true
requirements:
  - PRIV-03
  - PRIV-05

must_haves:
  truths:
    - "사용자가 '내 데이터 전체 삭제' 버튼을 클릭하면 Firebase 익명 계정이 삭제되고 sessionStorage와 Zustand store가 초기화된다"
    - "DELETE /api/account가 유효한 Firebase 토큰으로 호출되면 204를 반환하고 계정을 삭제한다"
    - "이혼 사유 스텝(스텝 4) 진입 전에 PIPA 민감정보 동의 다이얼로그가 표시된다"
    - "PIPA 동의를 거부하면 스텝 3에서 대기하고 스텝 4로 진행되지 않는다"
    - "PIPA 동의 상태가 Zustand store에 저장되며 reset() 호출 시 false로 초기화된다"
  artifacts:
    - path: "src/app/api/account/route.ts"
      provides: "DELETE endpoint for Firebase anonymous account deletion"
      exports: ["DELETE"]
    - path: "src/components/DeleteDataButton.tsx"
      provides: "내 데이터 전체 삭제 버튼"
      contains: "handleDelete"
    - path: "src/components/wizard/PipaConsentDialog.tsx"
      provides: "PIPA 민감정보 동의 모달"
      contains: "민감정보 처리 동의"
    - path: "src/stores/wizardStore.ts"
      provides: "pipaConsented 상태"
      contains: "pipaConsented"
    - path: "src/app/[locale]/layout.tsx"
      provides: "DeleteDataButton import 및 렌더링"
      contains: "DeleteDataButton"
  key_links:
    - from: "src/components/DeleteDataButton.tsx"
      to: "DELETE /api/account"
      via: "fetch with Bearer token"
      pattern: "fetch.*api/account.*DELETE"
    - from: "src/app/api/account/route.ts"
      to: "adminAuth.deleteUser"
      via: "firebase-admin Auth"
      pattern: "adminAuth\\.deleteUser"
    - from: "src/components/wizard/WizardContainer.tsx"
      to: "PipaConsentDialog"
      via: "handleNext 가드 at step 3→4"
      pattern: "pipaConsented"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/components/DeleteDataButton.tsx"
      via: "import and render"
      pattern: "DeleteDataButton"

user_setup: []
---

<objective>
PRIV-03(전체 삭제 버튼)과 PRIV-05(PIPA 민감정보 동의 게이트)를 구현하여 개인정보보호 요구사항을 완성한다.

Purpose: 사용자가 데이터를 완전 삭제할 수 있고, 민감정보 입력 전에 명시적 동의를 받아 PIPA 제23조를 준수한다.
Output: DELETE /api/account 엔드포인트, DeleteDataButton 컴포넌트, PipaConsentDialog 컴포넌트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md
@.planning/phases/03-polish-accessibility-pipa-completeness/03-01-SUMMARY.md

<interfaces>
<!-- Firebase Admin Auth (이미 존재) -->
From src/lib/firebaseAdmin.ts:
```typescript
export const adminAuth: Auth; // Proxy pattern - lazy init
// Usage: adminAuth.verifyIdToken(token) → { uid: string }
// Usage: adminAuth.deleteUser(uid) → void
```

<!-- 기존 analyze route의 토큰 검증 패턴 (복제할 것) -->
From src/app/api/analyze/route.ts:
```typescript
export const runtime = 'nodejs';
const authHeader = req.headers.get('Authorization');
if (!authHeader?.startsWith('Bearer ')) {
  return new Response(JSON.stringify({ error: '인증이 필요합니다.' }), { status: 401 });
}
const { uid } = await adminAuth.verifyIdToken(authHeader.slice(7));
```

<!-- WizardStore (수정 대상) -->
From src/stores/wizardStore.ts:
```typescript
interface WizardState {
  currentStep: number;
  situation: Partial<UserSituation>;
  isDvDetected: boolean;
  setStep: (step: number) => void;
  updateSituation: (partial: Partial<UserSituation>) => void;
  setDvDetected: (detected: boolean) => void;
  reset: () => void;
}
// initialState = { currentStep: 0, situation: {}, isDvDetected: false }
```

<!-- WizardContainer (수정 대상 — Plan 01에서 포커스 관리 추가됨) -->
From src/components/wizard/WizardContainer.tsx:
```typescript
interface WizardContainerProps {
  onComplete: (situation: UserSituation) => void;
}
// currentStep: 0=Intro, 1=MarriageDuration, 2=Children, 3=Asset, 4=DivorceReason, 5=SafetyBranch
// handleNext: step 3→4 전환 시 PIPA 동의 가드 추가 필요
```

<!-- AuthProvider (Firebase 인증 컨텍스트) -->
From src/components/AuthProvider.tsx:
```typescript
// Firebase anonymous auth 제공
// auth.currentUser?.getIdToken() 으로 토큰 획득 가능
```

<!-- locale layout (DeleteDataButton 배치 위치) -->
From src/app/[locale]/layout.tsx:
```typescript
// <PrivacyNotice /> 바로 위 또는 내부에 DeleteDataButton 배치
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DELETE /api/account 엔드포인트 + DeleteDataButton 컴포넌트 (PRIV-03)</name>
  <files>
    src/app/api/account/route.ts,
    src/app/api/account/__tests__/route.test.ts,
    src/components/DeleteDataButton.tsx,
    src/components/__tests__/DeleteDataButton.test.tsx,
    src/app/[locale]/layout.tsx
  </files>
  <read_first>
    src/app/api/analyze/route.ts,
    src/lib/firebaseAdmin.ts,
    src/components/PrivacyNotice.tsx,
    src/components/AuthProvider.tsx,
    src/stores/wizardStore.ts,
    src/app/[locale]/layout.tsx,
    .planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: DELETE /api/account — Authorization 헤더 없으면 401 반환
    - Test 2: DELETE /api/account — 유효한 토큰이면 adminAuth.deleteUser(uid) 호출 후 204 반환
    - Test 3: DELETE /api/account — verifyIdToken 실패 시 401 반환
    - Test 4: DELETE /api/account — deleteUser 실패 시 500 반환
    - Test 5: DeleteDataButton — 클릭 시 확인 다이얼로그 표시 ("정말 삭제하시겠습니까?")
    - Test 6: DeleteDataButton — 확인 후 fetch DELETE 호출 + sessionStorage.clear + store.reset 호출
  </behavior>
  <action>
    **1. Route Handler 생성 (`src/app/api/account/route.ts`):**
    ```typescript
    import { adminAuth } from '@/lib/firebaseAdmin';
    
    export const runtime = 'nodejs';
    
    export async function DELETE(req: Request) {
      const authHeader = req.headers.get('Authorization');
      if (!authHeader?.startsWith('Bearer ')) {
        return new Response(JSON.stringify({ error: '인증이 필요합니다.' }), {
          status: 401,
          headers: { 'Content-Type': 'application/json' },
        });
      }
      try {
        const { uid } = await adminAuth.verifyIdToken(authHeader.slice(7));
        await adminAuth.deleteUser(uid);
        return new Response(null, { status: 204 });
      } catch {
        return new Response(JSON.stringify({ error: '삭제에 실패했습니다.' }), {
          status: 500,
          headers: { 'Content-Type': 'application/json' },
        });
      }
    }
    ```

    **2. Route Handler 테스트 (`src/app/api/account/__tests__/route.test.ts`):**
    - `vi.mock('@/lib/firebaseAdmin')` 으로 adminAuth 모킹
    - 4개 케이스: 401 (no header), 204 (success), 401 (invalid token), 500 (delete fail)

    **3. DeleteDataButton 컴포넌트 (`src/components/DeleteDataButton.tsx`):**
    ```typescript
    'use client';
    import { useState } from 'react';
    import { getAuth } from 'firebase/auth';
    import { useWizardStore } from '@/stores/wizardStore';
    
    export function DeleteDataButton() {
      const [status, setStatus] = useState<'idle' | 'confirming' | 'deleting' | 'done' | 'error'>('idle');
      
      const handleClick = () => setStatus('confirming');
      const handleCancel = () => setStatus('idle');
      
      const handleConfirm = async () => {
        setStatus('deleting');
        try {
          const token = await getAuth().currentUser?.getIdToken();
          if (token) {
            const res = await fetch('/api/account', {
              method: 'DELETE',
              headers: { Authorization: `Bearer ${token}` },
            });
            if (!res.ok && res.status !== 204) throw new Error('삭제 실패');
          }
          sessionStorage.clear();
          useWizardStore.getState().reset();
          setStatus('done');
        } catch {
          setStatus('error');
        }
      };
      
      if (status === 'done') {
        return <p className="text-xs text-gray-600 text-center py-2">데이터가 삭제되었습니다.</p>;
      }
      
      return (
        <div className="text-center py-2">
          {status === 'confirming' ? (
            <div className="flex items-center justify-center gap-2">
              <span className="text-xs text-gray-700">정말 삭제하시겠습니까?</span>
              <button onClick={handleConfirm} disabled={status === 'deleting'}
                className="text-xs text-red-600 underline min-h-[48px] px-2">
                삭제
              </button>
              <button onClick={handleCancel}
                className="text-xs text-gray-600 underline min-h-[48px] px-2">
                취소
              </button>
            </div>
          ) : (
            <button onClick={handleClick}
              className="text-xs text-gray-600 underline min-h-[48px] px-2">
              내 데이터 전체 삭제
            </button>
          )}
          {status === 'error' && <p className="text-xs text-red-600 mt-1">삭제 중 오류가 발생했습니다.</p>}
        </div>
      );
    }
    ```

    **4. DeleteDataButton 테스트 (`src/components/__tests__/DeleteDataButton.test.tsx`):**
    - `vi.mock('firebase/auth')` + `vi.mock('@/stores/wizardStore')`
    - 클릭 → "정말 삭제하시겠습니까?" 표시 확인
    - 확인 → fetch mock 호출 + sessionStorage.clear + store.reset 호출 확인
    - 취소 → 원래 버튼으로 복귀 확인

    **5. locale layout에 배치:**
    `src/app/[locale]/layout.tsx`의 `<PrivacyNotice />` 바로 아래에 `<DeleteDataButton />` 추가.
    → layout.tsx 수정: `import { DeleteDataButton } from '@/components/DeleteDataButton';` 추가 후 `<DeleteDataButton />` 배치.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/account/__tests__/route.test.ts src/components/__tests__/DeleteDataButton.test.tsx --config ./vitest.config.ts --root .</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/account/route.ts` 파일이 존재하고 `export async function DELETE` 를 포함한다
    - `src/app/api/account/route.ts`에 `adminAuth.verifyIdToken` 호출이 있다
    - `src/app/api/account/route.ts`에 `adminAuth.deleteUser` 호출이 있다
    - `src/app/api/account/route.ts`에 `export const runtime = 'nodejs'`가 있다
    - `src/components/DeleteDataButton.tsx` 파일이 존재하고 `sessionStorage.clear()` 호출이 있다
    - `src/components/DeleteDataButton.tsx`에 `useWizardStore.getState().reset()` 호출이 있다
    - `src/components/DeleteDataButton.tsx`에 `내 데이터 전체 삭제` 문자열이 있다
    - `src/components/DeleteDataButton.tsx`에 `min-h-[48px]` 터치 타겟이 있다 (per UI-02)
    - `src/app/[locale]/layout.tsx`에 `DeleteDataButton` import와 렌더링이 포함되어 있다
    - route.test.ts, DeleteDataButton.test.tsx 모두 vitest 통과
  </acceptance_criteria>
  <done>사용자가 "내 데이터 전체 삭제" 버튼으로 Firebase 익명 계정 + sessionStorage + Zustand store를 한 번에 삭제할 수 있다. layout.tsx에 버튼이 배치되어 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PIPA 민감정보 동의 게이트 + wizardStore 확장 (PRIV-05)</name>
  <files>
    src/components/wizard/PipaConsentDialog.tsx,
    src/components/wizard/__tests__/PipaConsentDialog.test.tsx,
    src/stores/wizardStore.ts,
    src/components/wizard/WizardContainer.tsx
  </files>
  <read_first>
    src/components/wizard/WizardContainer.tsx,
    src/stores/wizardStore.ts,
    src/stores/__tests__/wizardStore.test.ts,
    .planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md,
    .planning/phases/03-polish-accessibility-pipa-completeness/03-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: wizardStore.pipaConsented 초기값이 false이다
    - Test 2: wizardStore.setPipaConsented(true) 호출 후 pipaConsented가 true이다
    - Test 3: wizardStore.reset() 호출 후 pipaConsented가 false로 리셋된다
    - Test 4: PipaConsentDialog가 open=true일 때 "민감정보 처리 동의" 제목이 표시된다
    - Test 5: "동의하고 계속하기" 버튼 클릭 시 onConsent 콜백이 호출된다
    - Test 6: "취소" 버튼 클릭 시 onCancel 콜백이 호출된다
    - Test 7: WizardContainer에서 step 3→4 전환 시 pipaConsented가 false이면 다이얼로그가 열린다
    - Test 8: WizardContainer에서 pipaConsented가 true이면 step 3→4가 바로 진행된다
  </behavior>
  <action>
    **0. 현재 WizardContainer.tsx 파악 (CRITICAL — Plan 01에서 수정됨):**
    먼저 현재 WizardContainer.tsx를 읽고 containerRef 및 기존 useEffect 위치를 파악한 후 pipaConsented 로직 삽입 위치를 결정한다. Plan 01에서 containerRef + 포커스 관리 useEffect가 추가되었으므로, 이를 건드리지 않고 PIPA 가드 로직만 추가해야 한다.

    **1. wizardStore 확장 (`src/stores/wizardStore.ts`):**
    - WizardState 인터페이스에 `pipaConsented: boolean;`과 `setPipaConsented: (consented: boolean) => void;` 추가
    - initialState에 `pipaConsented: false` 추가
    - create 내부에 `setPipaConsented: (consented) => set({ pipaConsented: consented }),` 추가
    - reset()에서 `pipaConsented: false` 포함 확인 (initialState spread로 이미 포함됨)

    **2. PipaConsentDialog 컴포넌트 (`src/components/wizard/PipaConsentDialog.tsx`):**
    shadcn Dialog 사용이 스택에 명시되어 있으나, shadcn init이 안 되어 있으므로 Radix Dialog를 직접 사용하거나 네이티브 dialog를 사용한다. 
    
    **실제 구현: 네이티브 HTML + Tailwind (shadcn 없이):**
    프로젝트에 shadcn이 초기화되지 않았으므로 (components.json 없음, src/components/ui/ 없음), init 실행 시 globals.css 덮어쓰기 위험이 있다 (Pitfall 1). 네이티브 dialog + 포커스 트랩을 직접 구현하되 Radix의 패턴을 따른다.
    
    ```tsx
    'use client';
    import { useEffect, useRef } from 'react';
    
    interface PipaConsentDialogProps {
      open: boolean;
      onConsent: () => void;
      onCancel: () => void;
    }
    
    export function PipaConsentDialog({ open, onConsent, onCancel }: PipaConsentDialogProps) {
      const dialogRef = useRef<HTMLDialogElement>(null);
      
      useEffect(() => {
        if (open) {
          dialogRef.current?.showModal();
        } else {
          dialogRef.current?.close();
        }
      }, [open]);
      
      // Escape 키 핸들링 (dialog 네이티브 cancel 이벤트)
      const handleCancel = (e: React.SyntheticEvent) => {
        e.preventDefault();
        onCancel();
      };
      
      if (!open) return null;
      
      return (
        <dialog
          ref={dialogRef}
          onCancel={handleCancel}
          className="fixed inset-0 z-50 m-auto max-w-md w-[calc(100%-2rem)] rounded-lg p-6 bg-white shadow-xl backdrop:bg-black/50"
          aria-labelledby="pipa-consent-title"
          aria-describedby="pipa-consent-desc"
        >
          <h2 id="pipa-consent-title" className="text-lg font-bold text-gray-900 mb-3">
            민감정보 처리 동의
          </h2>
          <p id="pipa-consent-desc" className="text-sm text-gray-700 mb-4 leading-relaxed">
            이후 입력하시는 '이혼 사유' 및 '가정폭력 여부' 정보는
            개인정보보호법 제23조의 민감정보에 해당할 수 있습니다.
          </p>
          <p className="text-sm text-gray-600 mb-6 leading-relaxed">
            이 정보는 법률 정보 검색 및 분석에만 사용되며,
            서버에 저장되지 않고 브라우저 세션 종료 시 삭제됩니다.
            AI 모델 학습에 사용되지 않습니다.
          </p>
          <div className="flex gap-3">
            <button
              onClick={onCancel}
              className="flex-1 py-3 border border-gray-300 rounded-lg text-gray-700 font-medium min-h-[48px]"
            >
              취소
            </button>
            <button
              onClick={onConsent}
              className="flex-1 py-3 bg-[#1B6B5A] text-white rounded-lg font-medium min-h-[48px]"
            >
              동의하고 계속하기
            </button>
          </div>
        </dialog>
      );
    }
    ```

    **3. WizardContainer 수정:**
    Plan 01 결과물(포커스 관리 포함된 WizardContainer)을 기반으로:
    - `import { PipaConsentDialog } from './PipaConsentDialog';` 추가
    - `const [showPipaConsent, setShowPipaConsent] = useState(false);` 추가
    - store에서 `pipaConsented, setPipaConsented` 구독 추가
    - `handleNext` 수정: `if (currentStep === 3 && !pipaConsented) { setShowPipaConsent(true); return; }` 가드 추가 (step 3=Asset → step 4=DivorceReason 전환 시)
    - `handlePipaConsent` 함수: `setPipaConsented(true); setShowPipaConsent(false); setStep(4);`
    - `handlePipaCancel` 함수: `setShowPipaConsent(false);` (스텝 3에서 대기)
    - JSX에 `<PipaConsentDialog open={showPipaConsent} onConsent={handlePipaConsent} onCancel={handlePipaCancel} />` 추가

    **4. 테스트 파일:**
    - `src/stores/__tests__/wizardStore.test.ts` 수정이 아닌, 기존 테스트에 pipaConsented 관련 케이스 추가 (또는 기존 파일에 test 추가)
    - `src/components/wizard/__tests__/PipaConsentDialog.test.tsx` 생성: render + 버튼 클릭 → 콜백 호출 검증

    **주의사항 (리서치 Pitfall 3):** `pipaConsented`를 sessionStorage에 저장하지 말 것 — Zustand store에만 저장하여 페이지 새로고침 시 리셋되도록 한다. `reset()` 호출 시에도 false로 리셋됨.
  </action>
  <verify>
    <automated>npx vitest run src/components/wizard/__tests__/PipaConsentDialog.test.tsx src/stores/__tests__/wizardStore.test.ts --config ./vitest.config.ts --root .</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/wizardStore.ts`에 `pipaConsented: false` 가 initialState에 포함되어 있다
    - `src/stores/wizardStore.ts`에 `setPipaConsented` 함수가 존재한다
    - `src/components/wizard/PipaConsentDialog.tsx` 파일이 존재하고 `민감정보 처리 동의` 문자열이 있다
    - `src/components/wizard/PipaConsentDialog.tsx`에 `개인정보보호법 제23조` 문자열이 있다
    - `src/components/wizard/PipaConsentDialog.tsx`에 `동의하고 계속하기` 버튼 텍스트가 있다
    - `src/components/wizard/PipaConsentDialog.tsx`에 `min-h-[48px]` 터치 타겟이 있다 (per UI-02)
    - `src/components/wizard/PipaConsentDialog.tsx`에 `aria-labelledby` 속성이 있다
    - `src/components/wizard/WizardContainer.tsx`에 `pipaConsented` 문자열이 있다
    - `src/components/wizard/WizardContainer.tsx`에 `PipaConsentDialog` import가 있다
    - `src/components/wizard/WizardContainer.tsx`에 `showPipaConsent` 상태가 있다
    - `src/components/wizard/WizardContainer.tsx`에 `currentStep === 3 && !pipaConsented` 가드가 있다
    - PipaConsentDialog.test.tsx 및 wizardStore.test.ts 모두 vitest 통과
  </acceptance_criteria>
  <done>이혼 사유 스텝 진입 전에 PIPA 동의 다이얼로그가 표시되고, 동의 없이는 진행 불가하며, reset 시 동의 상태가 초기화된다</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → DELETE /api/account | 인증된 사용자만 자신의 계정 삭제 가능해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | DELETE /api/account | mitigate | Bearer 토큰으로 adminAuth.verifyIdToken(token) 검증 — 토큰의 uid로만 해당 계정 삭제. 다른 사용자 계정 삭제 불가. |
| T-03-03 | Spoofing | DELETE /api/account | mitigate | Firebase ID Token은 Google 서명 검증을 거침. 위조 불가. 헤더 없으면 401 반환. |
| T-03-04 | Denial of Service | DELETE /api/account | accept | Rate limiting은 Vercel Edge 레벨에서 처리. 익명 계정 삭제는 자기 계정만이므로 DoS 영향 제한적. |
| T-03-05 | Information Disclosure | PIPA 동의 텍스트 | accept | 동의 문구에 민감정보 종류(이혼 사유, DV)가 명시됨 — 이는 PIPA 법적 요구사항이며 노출이 아닌 고지임. |
| T-03-06 | Tampering | pipaConsented 상태 | accept | Zustand 인메모리 상태. 클라이언트에서 조작 가능하나, 서버 측 법적 결론 차단(COMPL-03)이 독립적으로 동작하므로 동의 우회가 법적 위험으로 이어지지 않음. |
</threat_model>

<verification>
1. `npx vitest run --config ./vitest.config.ts --root .` — 전체 테스트 통과
2. `grep -r "adminAuth.deleteUser" src/app/api/account/route.ts` — 결과 1건
3. `grep -r "pipaConsented" src/stores/wizardStore.ts` — 결과 2건 이상
4. `grep -r "민감정보 처리 동의" src/components/wizard/PipaConsentDialog.tsx` — 결과 1건
5. `grep -r "currentStep === 3 && !pipaConsented" src/components/wizard/WizardContainer.tsx` — 결과 1건
6. `grep -r "DeleteDataButton" src/app/[locale]/layout.tsx` — 결과 1건 이상 (import + 렌더링)
</verification>

<success_criteria>
- "내 데이터 전체 삭제" 버튼이 layout.tsx에 배치되어 화면에 표시되고 클릭 시 Firebase 익명 계정 + 로컬 데이터가 삭제된다
- DELETE /api/account가 인증된 요청만 처리하고 204를 반환한다
- 위저드 스텝 3→4 전환 시 PIPA 동의 다이얼로그가 표시된다
- 동의 거부 시 스텝 3에서 대기하고, 동의 시 스텝 4로 진행된다
- pipaConsented가 Zustand store에만 저장되며 reset/새로고침 시 false로 리셋된다
- 모든 버튼이 min-h-[48px] 터치 타겟을 갖는다
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-accessibility-pipa-completeness/03-02-SUMMARY.md`
</output>
