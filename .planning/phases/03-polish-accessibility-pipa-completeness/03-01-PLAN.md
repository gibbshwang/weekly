---
phase: 03-polish-accessibility-pipa-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/wizard/DivorceReasonStep.tsx
  - src/components/wizard/ChildrenStep.tsx
  - src/components/wizard/WizardContainer.tsx
  - src/components/PrivacyNotice.tsx
  - src/app/[locale]/page.tsx
  - src/components/wizard/__tests__/WizardContainer.a11y.test.tsx
  - src/components/wizard/__tests__/DivorceReasonStep.a11y.test.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "375px 화면에서 위저드와 결과 보고서가 가로 스크롤 없이 완전히 표시된다"
    - "DivorceReasonStep 빠른 선택 버튼이 48px 이상 터치 타겟을 갖는다"
    - "키보드 Tab만으로 위저드 전체를 완료할 수 있다"
    - "스텝 전환 시 새 스텝 헤딩으로 포커스가 이동한다"
    - "모든 텍스트가 WCAG AA 대비율 4.5:1 이상을 충족한다"
    - "ChildrenStep textarea에 label이 htmlFor/id로 연결되어 있다"
    - "Pretendard 16px+ 기본 폰트가 적용되어 있고 보라색 계열(purple/violet/indigo)이 사용되지 않는다"
  artifacts:
    - path: "src/components/wizard/DivorceReasonStep.tsx"
      provides: "48px 터치 타겟 + aria-label on quick options"
      contains: "min-h-[48px]"
    - path: "src/components/wizard/WizardContainer.tsx"
      provides: "스텝 전환 시 포커스 이동"
      contains: "stepHeadingRef"
    - path: "src/components/wizard/ChildrenStep.tsx"
      provides: "label-textarea 연결"
      contains: "htmlFor"
    - path: "src/app/[locale]/page.tsx"
      provides: "색상 대비 수정"
      contains: "text-gray-600"
    - path: "src/components/PrivacyNotice.tsx"
      provides: "색상 대비 수정"
      contains: "text-gray-600"
  key_links:
    - from: "src/components/wizard/WizardContainer.tsx"
      to: "각 스텝 컴포넌트의 h2"
      via: "stepHeadingRef prop + useEffect focus"
      pattern: "stepHeadingRef.*focus"
---

<objective>
모바일 반응형, 터치 타겟, WCAG AA 접근성(색상 대비, 키보드 탐색, 라벨 연결, ARIA)을 수정하여 UI-01~UI-05 요구사항을 충족한다.

Purpose: Phase 2에서 구현된 위저드와 결과 UI의 접근성/반응형 결함 5개를 수정
Output: 접근성 테스트가 통과하는 위저드 컴포넌트들
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md

<interfaces>
<!-- WizardContainer의 현재 구조 -->
From src/components/wizard/WizardContainer.tsx:
```typescript
interface WizardContainerProps {
  onComplete: (situation: UserSituation) => void;
}
// currentStep: 0=Intro, 1=MarriageDuration, 2=Children, 3=Asset, 4=DivorceReason, 5=SafetyBranch
// TOTAL_STEPS = 5
// handleNext, handleBack, handleSafetyContinue
```

From src/stores/wizardStore.ts:
```typescript
interface WizardState {
  currentStep: number;
  situation: Partial<UserSituation>;
  isDvDetected: boolean;
  setStep: (step: number) => void;
  updateSituation: (partial: Partial<UserSituation>) => void;
  setDvDetected: (detected: boolean) => void;
  reset: () => void;
}
```

From src/components/wizard/DivorceReasonStep.tsx:
```typescript
interface DivorceReasonStepProps {
  onNext: () => void;
  onBack: () => void;
}
const QUICK_OPTIONS = ['성격 차이', '경제적 문제', '외도/부정행위', '가정폭력', '기타'];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 터치 타겟 48px 수정 + 반응형 검증 (UI-01, UI-02)</name>
  <files>
    src/components/wizard/DivorceReasonStep.tsx,
    src/components/wizard/__tests__/DivorceReasonStep.a11y.test.tsx
  </files>
  <read_first>
    src/components/wizard/DivorceReasonStep.tsx,
    src/components/wizard/__tests__/DivorceReasonStep.test.tsx,
    src/components/wizard/WizardContainer.tsx,
    .planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: DivorceReasonStep 빠른 선택 버튼에 `min-h-[48px]` 클래스가 있어야 한다
    - Test 2: 빠른 선택 버튼에 `aria-label`이 `이혼 사유 추가:` 접두사를 포함해야 한다
    - Test 3: 빠른 선택 버튼이 role="button"으로 렌더링된다 (기본 button 태그)
    - Test 4: WizardContainer의 위저드 wrapper에 `max-w-lg` 반응형 패턴이 존재하고 고정 width 값(예: `w-[500px]`)이 없어야 한다 (375px 반응형 보장)
  </behavior>
  <action>
    1. `src/components/wizard/__tests__/DivorceReasonStep.a11y.test.tsx` 생성:
       - 테스트 1: `render(<DivorceReasonStep onNext={vi.fn()} onBack={vi.fn()} />)` 후 QUICK_OPTIONS 각 버튼의 className에 `min-h-[48px]`이 포함되는지 검증 (container.querySelectorAll로 확인)
       - 테스트 2: `screen.getByLabelText('이혼 사유 추가: 성격 차이')`가 존재하는지 검증
       - 테스트 3: 모든 빠른 선택 버튼 5개가 렌더링되는지 검증
       - 테스트 4 (375px 반응형 검증): WizardContainer를 렌더링하고, wrapper div의 className에 `max-w-lg`가 포함되어 있는지 확인. 또한 `w-[` 패턴의 고정 width 클래스가 없는지 검증 — 이를 통해 375px viewport에서 가로 스크롤이 발생하지 않음을 보장한다.

    2. `src/components/wizard/DivorceReasonStep.tsx` 수정:
       - line 57: `min-h-[36px]` -> `min-h-[48px]` 변경
       - line 53-58의 button에 `aria-label={`이혼 사유 추가: ${option}`}` 추가
       - `py-2` -> `py-3` 변경 (48px 확보)

    (per UI-01, UI-02) 375px 반응형은 기존 `max-w-lg mx-auto px-4` 패턴으로 이미 충족 — 리서치에서 확인됨. 고정 너비나 수평 스크롤 발생 요소 없음. 테스트로 이 패턴 존재를 자동 검증한다.
  </action>
  <verify>
    <automated>npx vitest run src/components/wizard/__tests__/DivorceReasonStep.a11y.test.tsx --config ./vitest.config.ts --root .</automated>
  </verify>
  <acceptance_criteria>
    - DivorceReasonStep.tsx에 `min-h-[36px]` 문자열이 없다 (grep 결과 0건)
    - DivorceReasonStep.tsx에 `min-h-[48px]`이 빠른 선택 버튼에 존재한다
    - DivorceReasonStep.tsx에 `aria-label={` 문자열이 5개 버튼에 존재한다
    - DivorceReasonStep.a11y.test.tsx가 존재하고 vitest 실행 시 모든 테스트 통과
    - WizardContainer.tsx에 `max-w-lg` 반응형 패턴이 존재한다 (375px 대응 자동 검증됨)
  </acceptance_criteria>
  <done>DivorceReasonStep 빠른 선택 버튼이 48px 이상이고 스크린리더 접근 가능한 aria-label을 갖는다. 375px 반응형 패턴이 테스트로 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WCAG AA 색상 대비 + 키보드 포커스 관리 + 라벨 연결 + 금지 색상/폰트 검증 (UI-03, UI-04, UI-05)</name>
  <files>
    src/app/[locale]/page.tsx,
    src/components/PrivacyNotice.tsx,
    src/components/wizard/WizardContainer.tsx,
    src/components/wizard/ChildrenStep.tsx,
    src/components/wizard/__tests__/WizardContainer.a11y.test.tsx
  </files>
  <read_first>
    src/app/[locale]/page.tsx,
    src/components/PrivacyNotice.tsx,
    src/components/wizard/WizardContainer.tsx,
    src/components/wizard/ChildrenStep.tsx,
    src/components/wizard/__tests__/WizardContainer.test.tsx,
    .planning/phases/03-polish-accessibility-pipa-completeness/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: WizardContainer에서 currentStep이 변경되면 stepHeadingRef.current.focus()가 호출된다
    - Test 2: 각 스텝 컴포넌트의 h2에 tabIndex={-1}이 설정되어 있다
    - Test 3: ChildrenStep의 label이 htmlFor="childrenInfo"를 갖고 textarea가 id="childrenInfo"를 갖는다
    - Test 4: gray-500이 본문 텍스트에 사용되지 않는다 (page.tsx, PrivacyNotice.tsx)
  </behavior>
  <action>
    **색상 대비 수정 (UI-04):**
    1. `src/app/[locale]/page.tsx` line 23: `text-gray-500` -> `text-gray-600` 변경
       - gray-600 (#4B5563) on warm-gray (#F5F3F0) = 6.82:1 (WCAG AA PASS)
    2. `src/components/PrivacyNotice.tsx` line 2: `text-gray-500` -> `text-gray-600` 변경

    **키보드 포커스 관리 (UI-03):**
    3. `src/components/wizard/WizardContainer.tsx` 수정:
       - 상단에 `import { useRef, useEffect } from 'react';` 추가
       - 컴포넌트 내부에 `const stepHeadingRef = useRef<HTMLHeadingElement>(null);` 추가
       - `useEffect(() => { if (currentStep > 0) { stepHeadingRef.current?.focus(); } }, [currentStep]);` 추가
       - 각 스텝 컴포넌트에 `headingRef={stepHeadingRef}` prop 전달
       - 단, 각 스텝이 직접 ref를 받지 않으므로 대안: WizardContainer 내부에 wrapper div를 둘 필요 없이, 각 스텝의 h2에 직접 접근이 어렵다.
       
       **실제 구현 방법:** WizardContainer에서 스텝 전환 후 container 내 첫 번째 h2를 찾아 포커스:
       ```tsx
       const containerRef = useRef<HTMLDivElement>(null);
       useEffect(() => {
         if (currentStep > 0) {
           const heading = containerRef.current?.querySelector('h2');
           if (heading) {
             heading.setAttribute('tabindex', '-1');
             heading.focus();
           }
         }
       }, [currentStep]);
       ```
       그리고 최상위 `<div>`에 `ref={containerRef}` 추가.

    **라벨 연결 (UI-03):**
    4. `src/components/wizard/ChildrenStep.tsx` 수정:
       - line 70의 `<label>` 태그에 `htmlFor="childrenInfo"` 추가
       - line 73의 `<textarea>` 태그에 `id="childrenInfo"` 추가

    **테스트 파일:**
    5. `src/components/wizard/__tests__/WizardContainer.a11y.test.tsx` 생성:
       - 테스트: WizardContainer 렌더 후 store의 setStep(1) 호출 → container 내 h2가 document.activeElement인지 검증
       - 테스트: ChildrenStep 렌더 후 `screen.getByLabelText('자녀 나이와 현재 양육 상황을 알려주세요')`가 textarea 요소인지 검증

    (per UI-05) 정보 계층은 이미 올바른 순서: Landing(empathy→disclaimer→CTA) → Wizard → Results(IssueChecklist→StatuteSection→PrecedentSection→LawyerQuestions). 리서치에서 확인됨. 변경 불필요.
  </action>
  <verify>
    <automated>npx vitest run src/components/wizard/__tests__/WizardContainer.a11y.test.tsx --config ./vitest.config.ts --root .</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/[locale]/page.tsx`에 `text-gray-500` 문자열이 없다
    - `src/components/PrivacyNotice.tsx`에 `text-gray-500` 문자열이 없다
    - `src/app/[locale]/page.tsx`에 `text-gray-600` 문자열이 존재한다
    - `src/components/PrivacyNotice.tsx`에 `text-gray-600` 문자열이 존재한다
    - `src/components/wizard/WizardContainer.tsx`에 `containerRef` 또는 `stepHeadingRef` 문자열이 존재한다
    - `src/components/wizard/WizardContainer.tsx`에 `.focus()` 호출이 존재한다
    - `src/components/wizard/ChildrenStep.tsx`에 `htmlFor="childrenInfo"` 문자열이 존재한다
    - `src/components/wizard/ChildrenStep.tsx`에 `id="childrenInfo"` 문자열이 존재한다
    - WizardContainer.a11y.test.tsx가 존재하고 vitest 실행 시 모든 테스트 통과
    - `grep -ri "purple\|violet\|indigo" src/components/ src/app/` 결과 0건 (금지 색상 부재 확인, per UI-04)
    - `grep -r "text-xs\|text-\[12px\]\|text-\[13px\]\|text-\[14px\]\|text-\[15px\]" src/components/wizard/ src/app/[locale]/page.tsx` 결과에서 본문 텍스트가 16px 미만이면 수정 (Pretendard 16px+ 적용 확인, per UI-04)
  </acceptance_criteria>
  <done>모든 텍스트가 WCAG AA 대비율 충족, 키보드 포커스가 스텝 전환 시 이동, label-input 연결 완료, 금지 색상 부재 및 Pretendard 16px+ 확인됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 이 플랜은 클라이언트 UI 접근성 수정만 포함 — 서버/데이터 경계 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | 스텝 헤딩 포커스 | accept | 포커스 이동은 접근성 목적이며 민감 정보 노출 없음. 모든 헤딩은 이미 visible text. |
</threat_model>

<verification>
1. `npx vitest run --config ./vitest.config.ts --root .` — 전체 테스트 통과
2. `grep -r "min-h-\[36px\]" src/components/wizard/` — 결과 0건 (36px 제거 확인)
3. `grep -r "text-gray-500" src/app/ src/components/PrivacyNotice.tsx` — 결과 0건 (대비 실패 제거 확인)
4. `grep "htmlFor" src/components/wizard/ChildrenStep.tsx` — 결과 1건 이상
5. `grep -ri "purple\|violet\|indigo" src/components/ src/app/` — 결과 0건 (금지 색상 부재 확인)
</verification>

<success_criteria>
- 375px 화면에서 가로 스크롤 없이 위저드가 표시된다 (max-w-lg 패턴 테스트로 자동 검증)
- DivorceReasonStep 빠른 선택 버튼이 모두 min-h-[48px]이다
- 모든 본문 텍스트가 WCAG AA 대비율(4.5:1 이상)을 충족한다
- 스텝 전환 시 키보드 포커스가 새 스텝 헤딩으로 이동한다
- ChildrenStep textarea가 label과 htmlFor/id로 연결되어 있다
- 보라색 계열(purple/violet/indigo) 사용이 없다
- 본문 텍스트가 Pretendard 16px+ 기준을 충족한다
- 모든 접근성 테스트가 통과한다
</success_criteria>

<output>
After completion, create `.planning/phases/03-polish-accessibility-pipa-completeness/03-01-SUMMARY.md`
</output>
