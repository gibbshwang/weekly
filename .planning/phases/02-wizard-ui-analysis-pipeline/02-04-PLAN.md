---
phase: 02-wizard-ui-analysis-pipeline
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/results/ResultsContainer.tsx
  - src/components/results/IssueChecklist.tsx
  - src/components/results/StatuteSection.tsx
  - src/components/results/PrecedentSection.tsx
  - src/components/results/LawyerQuestions.tsx
  - src/components/results/SourceBadge.tsx
  - src/hooks/useAnalysisStream.ts
  - src/components/results/__tests__/ResultsContainer.test.tsx
  - src/components/results/__tests__/SourceBadge.test.tsx
autonomous: true
requirements:
  - ANALYSIS-03
  - ANALYSIS-05
  - LEGAL-01
  - LEGAL-02
  - LEGAL-03
  - LEGAL-04

must_haves:
  truths:
    - "스트리밍 텍스트가 도착하면 섹션별로 점진 렌더된다 (완료 대기 없음)"
    - "법령 섹션에 법령명 + 조문번호 출처가 표시된다"
    - "판례 섹션에 사건번호 + 선고일자가 표시된다"
    - "모든 법률 콘텐츠에 '법제처 API 검색 결과' 라벨이 표시된다"
    - "섹션 파싱 실패 시 원문 텍스트가 그대로 표시된다 (빈 화면 방지)"
  artifacts:
    - path: "src/components/results/ResultsContainer.tsx"
      provides: "Streaming results orchestrator"
      exports: ["ResultsContainer"]
    - path: "src/components/results/SourceBadge.tsx"
      provides: "법제처 API source label"
      exports: ["SourceBadge"]
    - path: "src/hooks/useAnalysisStream.ts"
      provides: "Streaming fetch + section parsing hook"
      exports: ["useAnalysisStream"]
  key_links:
    - from: "src/hooks/useAnalysisStream.ts"
      to: "/api/analyze"
      via: "fetch POST with Bearer token"
      pattern: "fetch.*api/analyze"
    - from: "src/hooks/useAnalysisStream.ts"
      to: "src/lib/parseAnalysisStream.ts"
      via: "parseAnalysisSections import"
      pattern: "import.*parseAnalysisSections"
    - from: "src/components/results/ResultsContainer.tsx"
      to: "src/hooks/useAnalysisStream.ts"
      via: "useAnalysisStream hook"
      pattern: "useAnalysisStream"
---

<objective>
스트리밍 분석 결과를 4개 섹션(쟁점/법령/판례/변호사질문)으로 점진 렌더하는 결과 UI 컴포넌트 전체를 구현한다.

Purpose: 사용자가 분석 완료를 기다리지 않고 도착하는 청크부터 바로 볼 수 있도록 스트리밍 결과 표시 UI를 구현. 모든 법률 콘텐츠에 출처 라벨을 부착하여 LEGAL-04를 충족.
Output: useAnalysisStream 훅 + ResultsContainer + 4개 섹션 컴포넌트 + SourceBadge + 테스트
</objective>

<execution_context>
@C:\Users\hhc20\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hhc20\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ui-analysis-pipeline/02-01-SUMMARY.md

<interfaces>
From src/lib/parseAnalysisStream.ts (Plan 01에서 생성):
```typescript
export interface AnalysisSections {
  issues: string[];
  statutes: string;
  precedents: string;
  lawyerQuestions: string[];
  rawText: string;
}
export function parseAnalysisSections(text: string): AnalysisSections;
```

From src/components/AuthProvider.tsx:
```typescript
export function useAuth(): { user: User | null; loading: boolean };
```

From src/types/analysis.ts:
```typescript
export interface UserSituation {
  marriageDuration: string;
  hasChildren: boolean;
  childrenInfo?: string;
  assetOverview?: string;
  divorceReason: string;
  additionalContext?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useAnalysisStream 훅 + SourceBadge 컴포넌트</name>
  <files>
    src/hooks/useAnalysisStream.ts,
    src/components/results/SourceBadge.tsx,
    src/components/results/__tests__/SourceBadge.test.tsx
  </files>
  <read_first>
    - src/lib/parseAnalysisStream.ts (Plan 01 생성 — AnalysisSections 타입과 parseAnalysisSections 함수 확인)
    - src/components/AuthProvider.tsx (useAuth 훅 시그니처)
    - src/app/api/analyze/route.ts (서버 응답 형식 — text/plain chunked)
  </read_first>
  <behavior>
    SourceBadge 테스트:
    - Test 1: SourceBadge 렌더 시 "법제처 API 검색 결과" 텍스트가 표시됨
    - Test 2: SourceBadge에 커스텀 className 전달 시 적용됨
  </behavior>
  <action>
    1. `src/components/results/__tests__/SourceBadge.test.tsx` 생성 — Test 1~2.
    2. `npm test -- SourceBadge` — RED.
    3. `src/components/results/SourceBadge.tsx` 구현:
       - `'use client'` 선언
       - Props: `{ className?: string }`
       - 렌더: `<span className={cn("inline-flex items-center gap-1 px-2 py-0.5 text-xs font-medium text-[#1B6B5A] bg-[#1B6B5A]/10 rounded-full", className)}>` + 아이콘(선택) + "법제처 API 검색 결과"
       - cn 유틸리티가 없으면 단순 문자열 결합으로 대체
    4. `npm test -- SourceBadge` — GREEN.
    5. `src/hooks/useAnalysisStream.ts` 구현:
       - `'use client'` 선언 불필요 (훅 파일)
       - 의존성: `useAuth()`, `parseAnalysisSections`, `useState`, `useCallback`
       - 인터페이스:
         ```typescript
         interface UseAnalysisStreamReturn {
           sections: AnalysisSections;
           rawText: string;
           isStreaming: boolean;
           error: string | null;
           startAnalysis: (situation: UserSituation) => Promise<void>;
         }
         ```
       - `startAnalysis` 구현:
         1. `user.getIdToken()` 으로 Bearer 토큰 취득
         2. `fetch('/api/analyze', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: \`Bearer ${idToken}\` }, body: JSON.stringify(situation) })`
         3. `response.ok` 확인 — 실패 시 `setError('분석 요청에 실패했습니다.')`
         4. `response.body!.getReader()` + `TextDecoder`로 청크 읽기
         5. 매 청크 수신 시 `buffer += chunk` → `parseAnalysisSections(buffer)` → `setSections(parsed)`
         6. 성능 최적화: 100ms debounce 또는 `requestAnimationFrame`으로 state 업데이트 배칭 (Pitfall 3 방지)
         7. done 시 `setIsStreaming(false)`
       - user가 null이면 `setError('인증이 필요합니다.')` 즉시 반환
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- SourceBadge</automated>
  </verify>
  <acceptance_criteria>
    - src/components/results/SourceBadge.tsx에 "법제처 API 검색 결과" 문자열 포함
    - src/hooks/useAnalysisStream.ts에 `fetch('/api/analyze'` 또는 `fetch(\`/api/analyze\`` 포함
    - src/hooks/useAnalysisStream.ts에 `parseAnalysisSections` import 포함
    - src/hooks/useAnalysisStream.ts에 `getIdToken` 호출 포함
    - src/hooks/useAnalysisStream.ts에 `getReader()` 호출 포함
    - `npm test -- SourceBadge` 종료코드 0
  </acceptance_criteria>
  <done>SourceBadge 2개 테스트 GREEN. useAnalysisStream 훅이 스트리밍 fetch + 섹션 파싱 + auth 토큰 처리 구현 완료.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResultsContainer + 4개 섹션 컴포넌트</name>
  <files>
    src/components/results/ResultsContainer.tsx,
    src/components/results/IssueChecklist.tsx,
    src/components/results/StatuteSection.tsx,
    src/components/results/PrecedentSection.tsx,
    src/components/results/LawyerQuestions.tsx,
    src/components/results/__tests__/ResultsContainer.test.tsx
  </files>
  <read_first>
    - src/hooks/useAnalysisStream.ts (Task 1에서 생성 — 반환값 인터페이스 확인)
    - src/lib/parseAnalysisStream.ts (AnalysisSections 타입)
    - src/components/results/SourceBadge.tsx (Task 1에서 생성)
    - src/components/LawyerCTA.tsx (기존 변호사 상담 CTA 컴포넌트 — 결과 하단에 재사용)
  </read_first>
  <behavior>
    - Test 1: ResultsContainer에 sections.issues=['항목1'] 전달 시 IssueChecklist가 렌더됨
    - Test 2: ResultsContainer에 sections.statutes 텍스트가 있으면 StatuteSection이 렌더됨
    - Test 3: isStreaming=true 상태에서 로딩 인디케이터가 표시됨
    - Test 4: 모든 섹션이 비어있고 rawText만 있으면 rawText가 그대로 표시됨 (fallback)
    - Test 5: StatuteSection과 PrecedentSection에 SourceBadge가 포함됨
  </behavior>
  <action>
    1. `src/components/results/__tests__/ResultsContainer.test.tsx` 생성 — Test 1~5. ResultsContainer는 props로 sections, isStreaming, rawText를 받도록 설계 (useAnalysisStream 결과를 부모가 전달).
    2. `npm test -- ResultsContainer` — RED.
    3. 섹션 컴포넌트 구현 (모두 `'use client'`):

    **IssueChecklist.tsx:**
    - Props: `{ issues: string[] }`
    - 제목: "쟁점 체크리스트"
    - 각 항목을 체크박스 스타일로 렌더 (실제 체크 불필요, 시각적 체크리스트)
    - 빈 배열이면 렌더하지 않음

    **StatuteSection.tsx:**
    - Props: `{ content: string }`
    - 제목: "관련 법령" + `<SourceBadge />` 라벨
    - content를 마크다운 스타일로 표시 (줄바꿈 유지, 볼드/리스트 기본 스타일링)
    - 빈 문자열이면 렌더하지 않음
    - LEGAL-01: 법령명 + 조문번호가 content에 포함됨 (Claude 프롬프트가 강제)

    **PrecedentSection.tsx:**
    - Props: `{ content: string }`
    - 제목: "관련 판례" + `<SourceBadge />` 라벨
    - content를 마크다운 스타일로 표시
    - LEGAL-02: 사건번호 + 선고일자가 content에 포함됨
    - 빈 문자열이면 렌더하지 않음

    **LawyerQuestions.tsx:**
    - Props: `{ questions: string[] }`
    - 제목: "변호사에게 물어볼 질문"
    - 각 질문을 번호 목록으로 렌더
    - 하단에 기존 `LawyerCTA` 컴포넌트 import하여 배치 (변호사 상담 권유)
    - 빈 배열이면 렌더하지 않음

    **ResultsContainer.tsx:**
    - Props: `{ sections: AnalysisSections; isStreaming: boolean; rawText: string; error: string | null }`
    - `'use client'` 선언
    - 레이아웃: 세로 스택, 각 섹션 간격 `gap-6`
    - 렌더 순서:
      1. error가 있으면 에러 메시지 표시 (빨간 배경)
      2. isStreaming이면 상단에 "분석 중..." 로딩 인디케이터 (펄스 애니메이션)
      3. sections.issues.length > 0 → `<IssueChecklist issues={sections.issues} />`
      4. sections.statutes → `<StatuteSection content={sections.statutes} />`
      5. sections.precedents → `<PrecedentSection content={sections.precedents} />`
      6. sections.lawyerQuestions.length > 0 → `<LawyerQuestions questions={sections.lawyerQuestions} />`
      7. 모든 섹션이 비어있고 rawText가 있으면 → rawText 그대로 표시 (fallback, Pitfall 5 방지)
    - 하단 면책 고지: "이 결과는 법률 정보 제공이며, 법률 자문이 아닙니다."
    - 배경: `bg-white rounded-xl p-6`, 제목/텍스트 색상: warm gray 계열

    4. `npm test -- ResultsContainer` — GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- ResultsContainer</automated>
  </verify>
  <acceptance_criteria>
    - src/components/results/ResultsContainer.tsx에 `IssueChecklist` import 포함
    - src/components/results/ResultsContainer.tsx에 `StatuteSection` import 포함
    - src/components/results/ResultsContainer.tsx에 `PrecedentSection` import 포함
    - src/components/results/ResultsContainer.tsx에 `LawyerQuestions` import 포함
    - src/components/results/StatuteSection.tsx에 `SourceBadge` import 포함
    - src/components/results/PrecedentSection.tsx에 `SourceBadge` import 포함
    - src/components/results/LawyerQuestions.tsx에 `LawyerCTA` import 또는 "변호사 상담" 텍스트 포함
    - src/components/results/ResultsContainer.tsx에 rawText fallback 로직 존재
    - `npm test -- ResultsContainer` 종료코드 0
  </acceptance_criteria>
  <done>ResultsContainer + 4개 섹션 컴포넌트 + SourceBadge 연동 완료. 5개 테스트 GREEN. 스트리밍 점진 렌더 + fallback 동작.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/analyze response → Client | 서버 응답 텍스트를 클라이언트에서 파싱하여 렌더 |
| Client → /api/analyze | 사용자 상황 데이터 + auth 토큰 전송 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Spoofing | useAnalysisStream | mitigate | Firebase auth 토큰을 Bearer 헤더로 전송. 토큰 없으면 분석 불가. 서버에서 verifyIdToken 검증 (Phase 1). |
| T-02-09 | Tampering | ResultsContainer | mitigate | 서버 응답 텍스트를 innerHTML이 아닌 텍스트 노드로 렌더. XSS 방지. React의 기본 이스케이핑 활용. dangerouslySetInnerHTML 절대 미사용. |
| T-02-10 | Information Disclosure | useAnalysisStream | mitigate | API 응답을 메모리(state)에만 보관. localStorage/sessionStorage 미사용. URL에 결과 데이터 미포함. |
</threat_model>

<verification>
```bash
cd C:/Users/hhc20/.openclaw/workspace && npm test -- ResultsContainer SourceBadge
```
</verification>

<success_criteria>
- useAnalysisStream: 스트리밍 fetch + 섹션 파싱 + auth 토큰 + 에러 처리
- ResultsContainer: 4개 섹션 순차 렌더 + fallback
- SourceBadge: "법제처 API 검색 결과" 라벨
- StatuteSection/PrecedentSection에 SourceBadge 포함 (LEGAL-04)
- LawyerQuestions에 변호사 상담 CTA 포함
- 7개 이상 테스트 GREEN
- dangerouslySetInnerHTML 미사용
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ui-analysis-pipeline/02-04-SUMMARY.md`
</output>
