---
phase: 02-wizard-ui-analysis-pipeline
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/pipelineOrchestrator.ts
  - src/lib/__tests__/pipelineOrchestrator.test.ts
  - package.json
autonomous: true
requirements:
  - ANALYSIS-01
  - ANALYSIS-02
  - ANALYSIS-04
  - ANALYSIS-06

must_haves:
  truths:
    - "buildUserPrompt 출력에 ## 쟁점 체크리스트, ## 관련 법령, ## 관련 판례, ## 변호사에게 물어볼 질문 4개 섹션 헤더가 포함된다"
    - "buildUserPrompt 출력에 '법제처 검색 결과에서만 인용' 지시가 포함된다"
    - "위저드 스텝에서 빈 값 제출 시 유효성 에러가 표시된다 (react-hook-form + zod 검증)"
  artifacts:
    - path: "src/lib/pipelineOrchestrator.ts"
      provides: "buildUserPrompt with structured section format"
      exports: ["buildUserPrompt", "orchestrateAnalysis"]
  key_links:
    - from: "src/lib/pipelineOrchestrator.ts"
      to: "src/lib/claudeClient.ts"
      via: "claudeClient.stream(userPrompt)"
      pattern: "claudeClient\\.stream"
    - from: "src/lib/pipelineOrchestrator.ts"
      to: "src/lib/koreanLawClient.ts"
      via: "searchStatutes + searchPrecedents"
      pattern: "searchStatutes|searchPrecedents"
---

<objective>
buildUserPrompt의 출력 형식을 파싱 가능한 4-섹션 마크다운 구조로 변경하고, react-hook-form 의존성을 설치한다.

Purpose: 클라이언트 스트리밍 파서(Plan 01)가 섹션 헤더 기준으로 파싱할 수 있도록 Claude 프롬프트 출력 구조를 표준화. react-hook-form 설치로 Plan 03(위저드 UI)의 의존성 준비.
Output: 구조화된 buildUserPrompt + react-hook-form 설치 완료
</objective>

<execution_context>
@C:\Users\hhc20\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hhc20\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From src/lib/pipelineOrchestrator.ts (현재 상태):
```typescript
export function buildUserPrompt(
  situation: UserSituation,
  statutes: Statute[],
  precedents: Precedent[],
): string { ... }

export function orchestrateAnalysis(situation: UserSituation): ReadableStream<Uint8Array> { ... }
```

From src/lib/claudeClient.ts:
```typescript
export const claudeClient = {
  stream(userPrompt: string): AsyncIterable<string> { ... }
};
export const SYSTEM_PROMPT: string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildUserPrompt 구조화 출력 형식 TDD</name>
  <files>src/lib/pipelineOrchestrator.ts, src/lib/__tests__/pipelineOrchestrator.test.ts</files>
  <read_first>
    - src/lib/pipelineOrchestrator.ts (현재 buildUserPrompt 구현 전체)
    - src/lib/__tests__/pipelineOrchestrator.test.ts (기존 테스트 확인 -- 깨뜨리지 않아야 함)
    - src/types/analysis.ts (UserSituation 타입)
    - src/types/law.ts (Statute, Precedent 타입)
  </read_first>
  <behavior>
    - Test 1: buildUserPrompt 결과에 "## 쟁점 체크리스트" 문자열이 포함된다
    - Test 2: buildUserPrompt 결과에 "## 관련 법령" 문자열이 포함된다
    - Test 3: buildUserPrompt 결과에 "## 관련 판례" 문자열이 포함된다
    - Test 4: buildUserPrompt 결과에 "## 변호사에게 물어볼 질문" 문자열이 포함된다
    - Test 5: buildUserPrompt 결과에 "섹션 헤더는 변경하지 마세요" 또는 "아래 형식으로 정확히 작성" 지시가 포함된다
    - Test 6: buildUserPrompt 결과에 "법제처 검색 결과에서만 인용" 지시가 포함된다
    - Test 7: 기존 테스트 -- statutes/precedents가 포함된 경우 법제처 API 검색 결과 섹션이 프롬프트에 포함됨
  </behavior>
  <action>
    1. `src/lib/__tests__/pipelineOrchestrator.test.ts`를 먼저 읽어서 기존 테스트 확인.
    2. 기존 테스트 파일에 위 7개 테스트 추가 (기존 테스트는 수정하지 않음). 테스트에서 사용할 mock 데이터:
       ```typescript
       const mockSituation: UserSituation = {
         marriageDuration: '10년',
         hasChildren: true,
         childrenInfo: '초등학생 2명',
         assetOverview: '아파트 1채',
         divorceReason: '성격 차이',
       };
       const mockStatutes: Statute[] = [{ name: '민법', mst: '001', category: '법률', effectiveDate: '2024-01-01' }];
       const mockPrecedents: Precedent[] = [{ caseNumber: '2023다12345', date: '2023-06-15', summary: '이혼 판결', ruling: '이혼 인용', precSeq: '1' }];
       ```
    3. `npm test -- pipelineOrchestrator` -- 새 테스트 RED, 기존 테스트 GREEN 확인.
    4. `src/lib/pipelineOrchestrator.ts`의 `buildUserPrompt` 수정:
       - 기존 `## 사용자 상황` 섹션과 `## 법제처 API 검색 결과` 섹션은 그대로 유지
       - 마지막 지시 부분(line 36~42)을 다음으로 교체:
         ```
         parts.push('\n아래 형식으로 정확히 작성해주세요. 섹션 헤더는 변경하지 마세요:');
         parts.push('\n## 쟁점 체크리스트');
         parts.push('(이혼 시 일반적으로 검토하는 쟁점을 - 기호로 체크리스트 항목으로. 개인화 결론 금지)');
         parts.push('\n## 관련 법령');
         parts.push('(위 법제처 검색 결과에서만 인용. 법령명과 조문번호 명시. 각 법령에 대해 쉬운 말 요약 포함)');
         parts.push('\n## 관련 판례');
         parts.push('(위 법제처 검색 결과에서만 인용. 사건번호와 선고일자 명시. 각 판례에 대해 쉬운 말 요약 포함)');
         parts.push('\n## 변호사에게 물어볼 질문');
         parts.push('(사용자 상황에 맞는 구체적인 질문 목록을 - 기호로)');
         ```
       - `orchestrateAnalysis` 함수는 수정하지 않음.
    5. `npm test -- pipelineOrchestrator` -- 모든 테스트 GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- pipelineOrchestrator</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/pipelineOrchestrator.ts의 buildUserPrompt 출력에 "## 쟁점 체크리스트" 문자열 포함
    - src/lib/pipelineOrchestrator.ts의 buildUserPrompt 출력에 "## 변호사에게 물어볼 질문" 문자열 포함
    - src/lib/pipelineOrchestrator.ts의 buildUserPrompt 출력에 "법제처 검색 결과에서만 인용" 포함
    - src/lib/__tests__/pipelineOrchestrator.test.ts에 새 테스트 최소 6개 추가
    - 기존 테스트가 깨지지 않음 -- `npm test -- pipelineOrchestrator` 종료코드 0
  </acceptance_criteria>
  <done>buildUserPrompt가 4-섹션 구조화 형식을 출력하고 기존+신규 테스트 모두 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: react-hook-form + @hookform/resolvers 설치</name>
  <files>package.json</files>
  <read_first>
    - package.json (현재 dependencies 확인 -- react-hook-form이 없는지 검증)
  </read_first>
  <action>
    1. `npm ls react-hook-form` 실행하여 미설치 확인.
    2. `npm install react-hook-form @hookform/resolvers` 실행.
    3. `npm ls react-hook-form @hookform/resolvers` 실행하여 설치 확인.
    4. `npm test` 실행하여 기존 테스트가 깨지지 않는지 확인.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm ls react-hook-form @hookform/resolvers</automated>
  </verify>
  <acceptance_criteria>
    - package.json의 dependencies에 "react-hook-form" 키가 존재
    - package.json의 dependencies에 "@hookform/resolvers" 키가 존재
    - `npm ls react-hook-form` 출력에 에러 없음
    - `npm test` 종료코드 0
  </acceptance_criteria>
  <done>react-hook-form ^7.x와 @hookform/resolvers ^3.x가 설치되고 기존 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> buildUserPrompt | 사용자 입력이 Claude 프롬프트에 포함됨 -- prompt injection 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | buildUserPrompt | mitigate | Phase 1 sanitizeInput(2000자 제한)이 route.ts에서 먼저 실행됨. buildUserPrompt는 sanitized 데이터만 수신. SYSTEM_PROMPT의 변호사법 가드레일 + complianceFilter 이중 안전망. |
| T-02-04 | Tampering | package.json | accept | react-hook-form은 npm 공식 레지스트리에서 설치. 주간 3M+ 다운로드의 신뢰 패키지. |
</threat_model>

<verification>
```bash
cd C:/Users/hhc20/.openclaw/workspace && npm test -- pipelineOrchestrator && npm ls react-hook-form @hookform/resolvers
```
</verification>

<success_criteria>
- buildUserPrompt가 4개 섹션 헤더를 포함하는 구조화 형식 출력
- 기존 pipelineOrchestrator 테스트 + 신규 테스트 모두 GREEN
- react-hook-form, @hookform/resolvers 설치 완료
- `npm test` 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ui-analysis-pipeline/02-02-SUMMARY.md`
</output>
