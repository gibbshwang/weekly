---
phase: 02-wizard-ui-analysis-pipeline
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - src/app/[locale]/wizard/page.tsx
  - src/app/[locale]/page.tsx
autonomous: false
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-04
  - ANALYSIS-05
  - LEGAL-03

must_haves:
  truths:
    - "/ko/wizard 경로에서 위저드가 렌더된다"
    - "위저드 완료 후 동일 페이지에서 스트리밍 결과가 표시된다 (URL 변경 없음)"
    - "랜딩 페이지에서 위저드 페이지로 이동하는 CTA가 존재한다"
    - "레이아웃의 QuickExitButton, DisclaimerBanner가 위저드 페이지에도 적용된다"
  artifacts:
    - path: "src/app/[locale]/wizard/page.tsx"
      provides: "Wizard page route under [locale]"
      exports: ["default"]
    - path: "src/app/[locale]/page.tsx"
      provides: "Updated landing page with wizard CTA"
      exports: ["default"]
  key_links:
    - from: "src/app/[locale]/wizard/page.tsx"
      to: "src/components/wizard/WizardContainer.tsx"
      via: "WizardContainer import"
      pattern: "import.*WizardContainer"
    - from: "src/app/[locale]/wizard/page.tsx"
      to: "src/components/results/ResultsContainer.tsx"
      via: "ResultsContainer import"
      pattern: "import.*ResultsContainer"
    - from: "src/app/[locale]/wizard/page.tsx"
      to: "src/hooks/useAnalysisStream.ts"
      via: "useAnalysisStream import"
      pattern: "import.*useAnalysisStream"
---

<objective>
위저드와 결과를 연결하는 페이지 라우트를 생성하고 랜딩 페이지에 위저드 CTA를 추가한다. 전체 플로우를 통합 검증한다.

Purpose: 사용자가 랜딩 → 위저드 입력 → 스트리밍 결과 표시까지의 전체 흐름을 경험할 수 있도록 최종 연결. URL 히스토리에 "이혼" 관련 흔적을 최소화.
Output: /wizard 페이지 + 업데이트된 랜딩 페이지 + 시각적 검증
</objective>

<execution_context>
@C:\Users\hhc20\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hhc20\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ui-analysis-pipeline/02-03-SUMMARY.md
@.planning/phases/02-wizard-ui-analysis-pipeline/02-04-SUMMARY.md

<interfaces>
From src/components/wizard/WizardContainer.tsx (Plan 03에서 생성):
```typescript
export function WizardContainer({ onComplete }: { onComplete: (situation: UserSituation) => void }): JSX.Element;
```

From src/components/results/ResultsContainer.tsx (Plan 04에서 생성):
```typescript
export function ResultsContainer({ sections, isStreaming, rawText, error }: {
  sections: AnalysisSections;
  isStreaming: boolean;
  rawText: string;
  error: string | null;
}): JSX.Element;
```

From src/hooks/useAnalysisStream.ts (Plan 04에서 생성):
```typescript
export function useAnalysisStream(): {
  sections: AnalysisSections;
  rawText: string;
  isStreaming: boolean;
  error: string | null;
  startAnalysis: (situation: UserSituation) => Promise<void>;
};
```

From src/app/[locale]/layout.tsx:
```typescript
// QuickExitButton + DisclaimerBanner + EmergencyContacts가 이미 포함된 레이아웃
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /wizard 페이지 라우트 + 랜딩 페이지 CTA</name>
  <files>src/app/[locale]/wizard/page.tsx, src/app/[locale]/page.tsx</files>
  <read_first>
    - src/app/[locale]/layout.tsx (레이아웃 구조 -- QuickExit, Disclaimer 포함 여부 확인)
    - src/app/[locale]/page.tsx (현재 랜딩 페이지 내용)
    - src/components/wizard/WizardContainer.tsx (onComplete prop 시그니처)
    - src/hooks/useAnalysisStream.ts (hook 반환값 확인)
    - src/components/results/ResultsContainer.tsx (props 확인)
  </read_first>
  <action>
    1. `src/app/[locale]/wizard/page.tsx` 생성:
       - `'use client'` 선언 (useAnalysisStream 훅 사용)
       - `import { WizardContainer } from '@/components/wizard/WizardContainer'`
       - `import { ResultsContainer } from '@/components/results/ResultsContainer'`
       - `import { useAnalysisStream } from '@/hooks/useAnalysisStream'`
       - `import type { UserSituation } from '@/types/analysis'`
       - 상태: `showResults: boolean` (false 초기값)
       - useAnalysisStream 훅 사용: `{ sections, rawText, isStreaming, error, startAnalysis }`
       - `handleComplete` 콜백:
         1. `setShowResults(true)`
         2. `startAnalysis(situation)` 호출
       - 렌더:
         - `!showResults` -> `<WizardContainer onComplete={handleComplete} />`
         - `showResults` -> `<ResultsContainer sections={sections} isStreaming={isStreaming} rawText={rawText} error={error} />`
         - showResults 상태에서 "다시 시작하기" 버튼: `setShowResults(false)` + wizardStore.reset()
       - URL push/replace 없음 -- 동일 URL에서 위저드<->결과 전환 (브라우저 히스토리 보호)
       - `[locale]` 하위에 있으므로 layout.tsx의 QuickExit + Disclaimer 자동 적용

    2. `src/app/[locale]/page.tsx` 수정:
       - 기존 내용 확인 후 위저드 CTA 추가
       - 추가할 내용:
         - 히어로 섹션: "이혼 법률 상담, 어디서부터 시작해야 할지 모르겠다면" (공감 헤드라인)
         - 설명: "4가지 질문에 답하면 관련 법령, 판례, 변호사에게 물어볼 질문을 정리해 드립니다."
         - CTA 버튼: `<Link href="/wizard">무료로 시작하기</Link>`, `className="inline-block px-8 py-4 bg-[#1B6B5A] text-white rounded-lg font-bold text-lg min-h-[48px]"`
         - 하단 면책: "법률 정보 제공 서비스이며, 법률 자문이 아닙니다."
         - next-intl의 `Link`를 사용하여 로케일 라우팅 호환: `import { Link } from '@/i18n/routing'` 또는 `import Link from 'next/link'` (기존 패턴 따름)

    3. `npm run build` 실행하여 빌드 에러 없는지 확인.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/wizard/page.tsx 파일이 존재하고 `'use client'` 선언 포함
    - src/app/[locale]/wizard/page.tsx에 `WizardContainer` import 포함
    - src/app/[locale]/wizard/page.tsx에 `ResultsContainer` import 포함
    - src/app/[locale]/wizard/page.tsx에 `useAnalysisStream` import 포함
    - src/app/[locale]/wizard/page.tsx에 `history.pushState` 또는 `router.push` 호출이 없음 (히스토리 보호)
    - src/app/[locale]/page.tsx에 `/wizard` 또는 `wizard` 링크 포함
    - src/app/[locale]/page.tsx에 "법률 자문이 아닙니다" 텍스트 포함
    - `npm run build` 종료코드 0
  </acceptance_criteria>
  <done>/wizard 페이지에서 위저드->결과 전환이 동작하고, 랜딩 페이지에 CTA가 표시되며, 빌드 성공.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 전체 플로우 시각적 검증</name>
  <files>N/A</files>
  <action>
    사용자가 dev 서버를 실행하고 전체 플로우를 시각적으로 검증한다.
    1. `npm run dev` 실행
    2. 아래 how-to-verify 단계를 순서대로 수행
    3. 이슈 발견 시 수정 사항을 resume-signal로 전달
  </action>
  <what-built>
    Phase 2 전체 기능: 랜딩 -> 위저드 5단계 입력 -> 스트리밍 결과 표시 플로우.
    - 랜딩 페이지에 위저드 CTA
    - 위저드: IntroStep(공감+면책) -> 결혼기간 -> 자녀 -> 재산 -> 이혼사유
    - DV 키워드("폭력" 등) 입력 시 SafetyBranch 표시
    - 위저드 완료 후 스트리밍 결과: 쟁점 체크리스트, 법령, 판례, 변호사 질문
    - "법제처 API 검색 결과" 출처 라벨
    - 이전 스텝 이동 시 입력값 유지
  </what-built>
  <how-to-verify>
    1. `cd C:/Users/hhc20/.openclaw/workspace && npm run dev` 실행
    2. http://localhost:3000/ko 방문 -- 랜딩 페이지에 위저드 CTA 버튼 확인
    3. CTA 클릭 -> /ko/wizard 이동 -- IntroStep 표시 확인 (공감 인트로 + 면책)
    4. "시작하기" 클릭 -> 결혼 기간 입력 -> "다음" -> 자녀 정보 -> "다음" -> 재산 -> "다음" -> 이혼 사유
    5. 이전 스텝 버튼으로 돌아가서 입력값이 유지되는지 확인 (INTAKE-03)
    6. DivorceReason에 "폭력" 입력 -> SafetyBranch(1366, 112) 표시 확인 (SAFE-04)
    7. "안전한 상황입니다" 클릭 -> 스트리밍 결과 표시 시작 확인
    8. 결과에 "법제처 API 검색 결과" 라벨 표시 확인 (LEGAL-04)
    9. QuickExitButton이 위저드/결과 페이지에서도 보이는지 확인
    10. 전체 과정에서 브라우저 URL에 민감한 쿼리 파라미터가 노출되지 않는지 확인

    참고: ANTHROPIC_API_KEY가 설정되어 있어야 Claude 스트리밍이 동작합니다. 미설정 시 에러 메시지가 표시됩니다.
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm run build</automated>
  </verify>
  <done>사용자가 전체 플로우를 확인하고 "approved" 응답.</done>
  <resume-signal>"approved" 또는 수정이 필요한 부분을 설명해 주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User browser -> /wizard page | 사용자가 위저드에 민감한 개인 상황을 입력 |
| /wizard page -> /api/analyze | 상황 데이터가 서버로 전송 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information Disclosure | wizard/page.tsx | mitigate | URL 변경 없음 (history.pushState 미사용). 브라우저 히스토리에 "이혼" 경로만 남고 쿼리 파라미터 없음. Zustand 메모리 전용. |
| T-02-12 | Information Disclosure | page.tsx (landing) | accept | 랜딩 페이지 URL(/ko/wizard)에는 "wizard"만 노출. 이혼 관련 단어 미포함. Phase 1에서 page title 중립 처리 완료. |
</threat_model>

<verification>
```bash
cd C:/Users/hhc20/.openclaw/workspace && npm run build && npm test
```
빌드 성공 + 전체 테스트 GREEN + 시각적 검증 통과.
</verification>

<success_criteria>
- /ko/wizard 경로에서 위저드 전체 플로우 동작
- 위저드 완료 후 동일 URL에서 스트리밍 결과 표시
- 랜딩 페이지에 위저드 CTA 표시
- 레이아웃(QuickExit, Disclaimer, EmergencyContacts) 위저드에 적용
- DV 감지 -> SafetyBranch -> 계속 진행 플로우 동작
- npm run build 성공
- npm test 전체 GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ui-analysis-pipeline/02-05-SUMMARY.md`
</output>
