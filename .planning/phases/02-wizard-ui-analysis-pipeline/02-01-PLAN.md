---
phase: 02-wizard-ui-analysis-pipeline
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/stores/wizardStore.ts
  - src/stores/__tests__/wizardStore.test.ts
  - src/lib/dvDetector.ts
  - src/lib/__tests__/dvDetector.test.ts
  - src/lib/parseAnalysisStream.ts
  - src/lib/__tests__/parseAnalysisStream.test.ts
autonomous: true
requirements:
  - INTAKE-03
  - ANALYSIS-03
  - ANALYSIS-05
  - SAFE-04

must_haves:
  truths:
    - "Zustand wizardStore에 step 데이터를 저장하면 step 이동 후에도 값이 유지된다"
    - "DV 키워드(폭력, 학대, 협박 등)가 포함된 텍스트를 detectDv에 전달하면 true가 반환된다"
    - "## 섹션명 구분자가 포함된 스트리밍 텍스트를 파싱하면 4개 섹션(쟁점/법령/판례/변호사질문)으로 분리된다"
  artifacts:
    - path: "src/stores/wizardStore.ts"
      provides: "Zustand v5 curried wizard state store"
      exports: ["useWizardStore"]
    - path: "src/lib/dvDetector.ts"
      provides: "DV keyword detection utility"
      exports: ["detectDv", "DV_PATTERNS"]
    - path: "src/lib/parseAnalysisStream.ts"
      provides: "Streaming text section parser"
      exports: ["parseAnalysisSections", "AnalysisSections"]
  key_links:
    - from: "src/stores/wizardStore.ts"
      to: "src/types/analysis.ts"
      via: "UserSituation import"
      pattern: "import.*UserSituation.*from.*@/types/analysis"
    - from: "src/lib/parseAnalysisStream.ts"
      to: "## 쟁점 체크리스트"
      via: "section header regex split"
      pattern: "##\\s+(쟁점|관련 법령|관련 판례|변호사)"
---

<objective>
Phase 2의 핵심 순수 로직 3가지를 TDD로 구현: (1) Zustand v5 위저드 상태 스토어, (2) DV 키워드 감지 유틸리티, (3) 스트리밍 텍스트 섹션 파서.

Purpose: 위저드 UI와 결과 표시 UI가 의존하는 상태 관리 + 파싱 로직을 먼저 TDD로 확립하여 UI 컴포넌트가 검증된 계약에 기반해 구현될 수 있도록 한다.
Output: 테스트가 통과하는 wizardStore.ts, dvDetector.ts, parseAnalysisStream.ts
</objective>

<execution_context>
@C:\Users\hhc20\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hhc20\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Phase 1에서 이미 정의된 타입. 이 타입들을 그대로 사용할 것. -->

From src/types/analysis.ts:
```typescript
export interface UserSituation {
  marriageDuration: string;
  hasChildren: boolean;
  childrenInfo?: string;
  assetOverview?: string;
  divorceReason: string;
  additionalContext?: string;
}

export interface AnalysisResult {
  issueChecklist: string[];
  relevantStatutes: Statute[];
  relevantPrecedents: Precedent[];
  lawyerQuestions: string[];
  disclaimer: string;
}

export interface AnalysisStreamChunk {
  type: 'text' | 'error' | 'done';
  content: string;
}
```

From src/types/law.ts:
```typescript
export interface Statute {
  name: string;
  mst: string;
  category: string;
  effectiveDate: string;
}

export interface Precedent {
  caseNumber: string;
  date: string;
  summary: string;
  ruling: string;
  precSeq?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zustand v5 위저드 스토어 TDD</name>
  <files>src/stores/wizardStore.ts, src/stores/__tests__/wizardStore.test.ts</files>
  <read_first>
    - src/types/analysis.ts (UserSituation 타입 확인)
    - vitest.config.ts (테스트 환경 설정 확인)
  </read_first>
  <behavior>
    - Test 1: useWizardStore 초기 상태는 currentStep=0, situation={}, isDvDetected=false
    - Test 2: setStep(2) 호출 후 currentStep === 2
    - Test 3: updateSituation({ marriageDuration: '5년' }) 호출 후 situation.marriageDuration === '5년'
    - Test 4: updateSituation을 여러 번 호출하면 기존 값이 유지되며 새 값만 병합 (INTAKE-03 핵심)
    - Test 5: setDvDetected(true) 호출 후 isDvDetected === true
    - Test 6: reset() 호출 후 모든 상태가 초기값으로 복원
    - Test 7: updateSituation 후 setStep으로 스텝 변경해도 situation 값 유지 (INTAKE-03 검증)
  </behavior>
  <action>
    1. `src/stores/__tests__/wizardStore.test.ts` 생성 — 위 7개 테스트 작성. Zustand 테스트는 직접 import하여 `useWizardStore.getState()`, `useWizardStore.setState()` 패턴으로 테스트 (React 렌더 불필요).
    2. `npm test -- wizardStore` 실행 — 모든 테스트 RED 확인.
    3. `src/stores/wizardStore.ts` 구현:
       - `import { create } from 'zustand'`
       - `import type { UserSituation } from '@/types/analysis'`
       - WizardState 인터페이스: `currentStep: number`, `situation: Partial<UserSituation>`, `isDvDetected: boolean`, `setStep`, `updateSituation`, `setDvDetected`, `reset`
       - Zustand v5 curried 형식 필수: `export const useWizardStore = create<WizardState>()((set) => ({ ... }))`
       - `updateSituation`은 spread merge: `set((state) => ({ situation: { ...state.situation, ...partial } }))`
       - localStorage 미사용 (DV 피해자 보안). persist 미들웨어 절대 금지.
    4. `npm test -- wizardStore` 실행 — 모든 테스트 GREEN 확인.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- wizardStore</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/wizardStore.ts 파일이 존재하고 `export const useWizardStore = create<WizardState>()(` 패턴 포함
    - src/stores/wizardStore.ts에 `import type { UserSituation } from '@/types/analysis'` 포함
    - src/stores/wizardStore.ts에 `localStorage` 또는 `persist` 문자열이 없음
    - src/stores/__tests__/wizardStore.test.ts에 최소 7개 test/it 블록
    - `npm test -- wizardStore` 종료코드 0
  </acceptance_criteria>
  <done>wizardStore의 7개 테스트 모두 GREEN. updateSituation 병합과 reset이 정상 동작.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 스트리밍 텍스트 섹션 파서 TDD</name>
  <files>src/lib/parseAnalysisStream.ts, src/lib/__tests__/parseAnalysisStream.test.ts</files>
  <read_first>
    - src/types/analysis.ts (AnalysisResult 타입 확인 — 파서 출력이 이 구조와 호환되어야 함)
    - src/lib/pipelineOrchestrator.ts (buildUserPrompt의 요청 형식 확인)
  </read_first>
  <behavior>
    - Test 1: "## 쟁점 체크리스트\n- 항목1\n- 항목2\n## 관련 법령\n법령내용" 입력 시 issues=['항목1','항목2'], statutes='법령내용'
    - Test 2: 섹션 헤더가 없는 원문 텍스트 → fallback으로 rawText에 전체 텍스트 반환
    - Test 3: 4개 섹션 모두 포함된 완전한 응답 파싱 성공
    - Test 4: 스트리밍 중간 상태 (일부 섹션만 도착) → 도착한 섹션만 채워지고 나머지는 빈 값
    - Test 5: 빈 문자열 입력 → 모든 필드 빈 값, rawText도 빈 문자열
    - Test 6: "## 쟁점 체크리스트" 뒤에 번호 목록(1. 항목) 형식도 파싱 가능
  </behavior>
  <action>
    1. `src/lib/__tests__/parseAnalysisStream.test.ts` 생성 — 위 6개 테스트.
    2. `npm test -- parseAnalysisStream` 실행 — RED.
    3. `src/lib/parseAnalysisStream.ts` 구현:
       - `AnalysisSections` 인터페이스 export: `{ issues: string[], statutes: string, precedents: string, lawyerQuestions: string[], rawText: string }`
       - `parseAnalysisSections(text: string): AnalysisSections` 함수 export
       - 파싱 로직: `## 쟁점 체크리스트`, `## 관련 법령`, `## 관련 판례`, `## 변호사에게 물어볼 질문` 섹션 헤더 기준으로 split
       - `- ` 또는 숫자 `1. ` 시작하는 라인은 배열 항목으로 파싱 (issues, lawyerQuestions)
       - statutes, precedents는 해당 섹션의 원문 텍스트 그대로 유지 (UI에서 마크다운 렌더)
       - 섹션 헤더가 하나도 없으면 rawText에 전체 텍스트, 나머지 빈 값 (fallback)
    4. `npm test -- parseAnalysisStream` — GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- parseAnalysisStream</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/parseAnalysisStream.ts에 `export interface AnalysisSections` 포함
    - src/lib/parseAnalysisStream.ts에 `export function parseAnalysisSections` 포함
    - src/lib/__tests__/parseAnalysisStream.test.ts에 최소 6개 test/it 블록
    - `npm test -- parseAnalysisStream` 종료코드 0
  </acceptance_criteria>
  <done>parseAnalysisSections의 6개 테스트 모두 GREEN. 4개 섹션 파싱 + fallback 동작.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DV 키워드 감지 유틸리티 TDD</name>
  <files>src/lib/dvDetector.ts, src/lib/__tests__/dvDetector.test.ts</files>
  <read_first>
    - src/lib/pipelineOrchestrator.ts (buildSearchKeywords 내 기존 DV 패턴 참고: /폭력|폭행|학대/)
  </read_first>
  <behavior>
    - Test 1: "남편이 폭력을 행사합니다" → true
    - Test 2: "성격 차이로 이혼하고 싶습니다" → false
    - Test 3: "학대와 협박이 있었습니다" → true
    - Test 4: "외도 때문입니다" → false
    - Test 5: "" (빈 문자열) → false
    - Test 6: "무섭고 두려워서 집을 나왔습니다" → true
    - Test 7: "성폭력 피해를 당했습니다" → true
    - Test 8: DV_PATTERNS 배열이 export되어 접근 가능
  </behavior>
  <action>
    1. `src/lib/__tests__/dvDetector.test.ts` 생성 — 위 8개 테스트.
    2. `npm test -- dvDetector` 실행 — RED.
    3. `src/lib/dvDetector.ts` 구현:
       - `DV_PATTERNS` export: `[/폭력|폭행|학대|때리|맞/, /협박|위협|무서|두렵/, /성폭력|성추행|성희롱/, /가정폭력/]`
       - `detectDv(text: string): boolean` export: `DV_PATTERNS.some((pattern) => pattern.test(text))`
       - 빈 문자열은 어떤 패턴에도 매칭되지 않으므로 자연스럽게 false 반환
    4. `npm test -- dvDetector` — GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- dvDetector</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/dvDetector.ts에 `export const DV_PATTERNS` 포함
    - src/lib/dvDetector.ts에 `export function detectDv` 포함
    - src/lib/__tests__/dvDetector.test.ts에 최소 8개 test/it 블록
    - `npm test -- dvDetector` 종료코드 0
  </acceptance_criteria>
  <done>detectDv의 8개 테스트 모두 GREEN. DV 키워드 7종 감지 + false positive 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zustand store → UI | Store data is untrusted user input; UI must not use it for security decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | wizardStore.ts | mitigate | localStorage/persist 미사용. Zustand 메모리 전용. 브라우저 종료 시 자동 삭제. |
| T-02-02 | Tampering | dvDetector.ts | accept | false positive는 UX 불편만 초래 (안전 분기 추가 표시). false negative는 Phase 1 compliance filter가 이중 안전망. |
</threat_model>

<verification>
```bash
cd C:/Users/hhc20/.openclaw/workspace && npm test -- wizardStore dvDetector parseAnalysisStream
```
전체 테스트 21개 이상 GREEN.
</verification>

<success_criteria>
- wizardStore: 7개 테스트 GREEN, Zustand v5 curried 패턴, localStorage 미사용
- parseAnalysisStream: 6개 테스트 GREEN, 4개 섹션 파싱 + fallback
- dvDetector: 8개 테스트 GREEN, 7종 DV 키워드 감지
- 기존 Phase 1 테스트가 깨지지 않음 (`npm test` 전체 GREEN)
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ui-analysis-pipeline/02-01-SUMMARY.md`
</output>
