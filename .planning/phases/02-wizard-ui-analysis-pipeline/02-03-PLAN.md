---
phase: 02-wizard-ui-analysis-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/wizard/IntroStep.tsx
  - src/components/wizard/MarriageDurationStep.tsx
  - src/components/wizard/ChildrenStep.tsx
  - src/components/wizard/AssetStep.tsx
  - src/components/wizard/DivorceReasonStep.tsx
  - src/components/wizard/SafetyBranch.tsx
  - src/components/wizard/WizardContainer.tsx
  - src/components/wizard/WizardProgress.tsx
  - src/components/wizard/__tests__/WizardContainer.test.tsx
  - src/components/wizard/__tests__/IntroStep.test.tsx
  - src/components/wizard/__tests__/DivorceReasonStep.test.tsx
autonomous: true
requirements:
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
  - INTAKE-04
  - SAFE-04

must_haves:
  truths:
    - "위저드가 IntroStep(공감+면책) → MarriageDuration → Children → Asset → DivorceReason 순서로 5단계 표시된다"
    - "각 스텝에서 입력한 데이터가 이전 스텝으로 돌아가도 유지된다"
    - "DivorceReasonStep에서 DV 키워드 입력 시 SafetyBranch가 표시된다"
    - "각 스텝이 1질문/1화면 구조로 일상 언어 질문을 사용한다"
  artifacts:
    - path: "src/components/wizard/WizardContainer.tsx"
      provides: "Step navigation + progress indicator"
      exports: ["WizardContainer"]
    - path: "src/components/wizard/IntroStep.tsx"
      provides: "Empathetic intro + disclaimer (INTAKE-04)"
      exports: ["IntroStep"]
    - path: "src/components/wizard/SafetyBranch.tsx"
      provides: "DV safety information (SAFE-04)"
      exports: ["SafetyBranch"]
  key_links:
    - from: "src/components/wizard/WizardContainer.tsx"
      to: "src/stores/wizardStore.ts"
      via: "useWizardStore hook"
      pattern: "useWizardStore"
    - from: "src/components/wizard/DivorceReasonStep.tsx"
      to: "src/lib/dvDetector.ts"
      via: "detectDv import"
      pattern: "import.*detectDv.*from.*dvDetector"
---

<objective>
4단계 위저드 UI 컴포넌트 전체를 구현: IntroStep(공감 인트로), MarriageDurationStep, ChildrenStep, AssetStep, DivorceReasonStep, SafetyBranch, WizardContainer(네비게이션+진행표시).

Purpose: 사용자가 일상 언어로 자신의 상황을 단계별로 입력할 수 있는 위저드 인터페이스 완성. DV 감지 시 안전 정보 브랜치가 분석 전에 표시됨.
Output: 렌더 가능한 위저드 컴포넌트 세트 + 핵심 테스트
</objective>

<execution_context>
@C:\Users\hhc20\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hhc20\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wizard-ui-analysis-pipeline/02-01-SUMMARY.md

<interfaces>
From src/stores/wizardStore.ts (Plan 01에서 생성):
```typescript
interface WizardState {
  currentStep: number;           // 0=Intro, 1=Marriage, 2=Children, 3=Asset, 4=DivorceReason
  situation: Partial<UserSituation>;
  isDvDetected: boolean;
  setStep: (step: number) => void;
  updateSituation: (partial: Partial<UserSituation>) => void;
  setDvDetected: (detected: boolean) => void;
  reset: () => void;
}
export const useWizardStore = create<WizardState>()((set) => ({ ... }));
```

From src/lib/dvDetector.ts (Plan 01에서 생성):
```typescript
export const DV_PATTERNS: RegExp[];
export function detectDv(text: string): boolean;
```

From src/types/analysis.ts:
```typescript
export interface UserSituation {
  marriageDuration: string;
  hasChildren: boolean;
  childrenInfo?: string;
  assetOverview?: string;
  divorceReason: string;
  additionalContext?: string;
}
```

From src/components/AuthProvider.tsx:
```typescript
export function useAuth(): { user: User | null; loading: boolean };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: IntroStep + WizardProgress 컴포넌트</name>
  <files>
    src/components/wizard/IntroStep.tsx,
    src/components/wizard/WizardProgress.tsx,
    src/components/wizard/__tests__/IntroStep.test.tsx
  </files>
  <read_first>
    - src/components/DisclaimerBanner.tsx (기존 면책 고지 컴포넌트 — 중복 방지, 스타일 참고)
    - src/app/[locale]/layout.tsx (레이아웃에 이미 포함된 컴포넌트 확인)
    - src/stores/wizardStore.ts (Plan 01에서 생성된 스토어 확인)
  </read_first>
  <behavior>
    - Test 1: IntroStep 렌더 시 "변호사 상담을 준비" 또는 "법률 정보 제공" 텍스트가 포함됨 (INTAKE-04)
    - Test 2: IntroStep 렌더 시 "법률 자문이 아닙니다" 면책 텍스트가 포함됨
    - Test 3: IntroStep의 "시작하기" 버튼 클릭 시 onNext 콜백 호출
    - Test 4: WizardProgress에 current=2, total=5 전달 시 40% 너비 프로그레스바 렌더
  </behavior>
  <action>
    1. `src/components/wizard/__tests__/IntroStep.test.tsx` 생성 — 위 4개 테스트.
    2. `npm test -- IntroStep` — RED.
    3. `src/components/wizard/WizardProgress.tsx` 구현:
       - `'use client'` 선언
       - Props: `{ current: number; total: number }`
       - 커스텀 프로그레스바 (shadcn/ui Progress 미사용 — CLAUDE.md 지시)
       - 외부 컨테이너: `className="w-full bg-[#E8E4E0] rounded-full h-1.5"`
       - 내부 바: `className="bg-[#1B6B5A] h-1.5 rounded-full transition-all duration-300"` + `style={{ width: \`${(current / total) * 100}%\` }}`
       - "N / M 단계" 텍스트 표시
    4. `src/components/wizard/IntroStep.tsx` 구현:
       - `'use client'` 선언
       - Props: `{ onNext: () => void }`
       - 공감 인트로 텍스트: "이혼을 고려하고 계신 상황이 쉽지 않으시죠. 이 도구는 변호사 상담을 준비하는 데 도움을 드립니다."
       - 면책 고지: "이 서비스는 법률 정보 제공 도구이며, 법률 자문이 아닙니다. 최종 판단은 반드시 변호사와 상담하세요."
       - 도구 설명: 4가지 질문 → 쟁점 체크리스트 + 관련 법령/판례 + 변호사 질문 목록
       - "시작하기" 버튼: `onClick={onNext}`, `className="w-full py-4 bg-[#1B6B5A] text-white rounded-lg font-bold text-lg min-h-[48px]"`
       - 배경: `bg-[#F5F3F0]`, 텍스트: Pretendard 16px+
    5. `npm test -- IntroStep` — GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- IntroStep</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/IntroStep.tsx에 "법률 자문이 아닙니다" 문자열 포함
    - src/components/wizard/IntroStep.tsx에 "변호사 상담" 문자열 포함
    - src/components/wizard/IntroStep.tsx에 `onNext` prop 사용
    - src/components/wizard/WizardProgress.tsx에 `bg-[#1B6B5A]` 포함
    - src/components/wizard/WizardProgress.tsx에 `bg-[#E8E4E0]` 포함
    - `npm test -- IntroStep` 종료코드 0
  </acceptance_criteria>
  <done>IntroStep이 공감 인트로 + 면책 + 시작 버튼을 렌더하고 4개 테스트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4개 위저드 스텝 컴포넌트 + WizardContainer</name>
  <files>
    src/components/wizard/MarriageDurationStep.tsx,
    src/components/wizard/ChildrenStep.tsx,
    src/components/wizard/AssetStep.tsx,
    src/components/wizard/DivorceReasonStep.tsx,
    src/components/wizard/SafetyBranch.tsx,
    src/components/wizard/WizardContainer.tsx,
    src/components/wizard/__tests__/WizardContainer.test.tsx,
    src/components/wizard/__tests__/DivorceReasonStep.test.tsx
  </files>
  <read_first>
    - src/stores/wizardStore.ts (스토어 인터페이스 확인)
    - src/lib/dvDetector.ts (detectDv 함수 시그니처)
    - src/types/analysis.ts (UserSituation 필드 확인)
    - src/components/EmergencyContacts.tsx (기존 1366/112 컴포넌트 스타일 참고)
  </read_first>
  <behavior>
    WizardContainer 테스트:
    - Test 1: 초기 렌더 시 IntroStep(step 0)이 표시됨
    - Test 2: step 1 상태에서 MarriageDurationStep이 표시됨
    - Test 3: "이전" 버튼 클릭 시 이전 스텝으로 이동
    DivorceReasonStep 테스트:
    - Test 4: DV 키워드("폭력") 입력 후 제출 시 isDvDetected가 true로 설정됨
    - Test 5: 일반 사유("성격 차이") 입력 후 제출 시 isDvDetected가 false
    - Test 6: SafetyBranch가 표시되면 "안전한 상황입니다" 버튼이 보임
  </behavior>
  <action>
    1. 테스트 파일 먼저 생성:
       - `src/components/wizard/__tests__/WizardContainer.test.tsx` — Test 1~3
       - `src/components/wizard/__tests__/DivorceReasonStep.test.tsx` — Test 4~6
    2. `npm test -- WizardContainer DivorceReasonStep` — RED.
    3. 각 스텝 컴포넌트 구현 (모두 `'use client'`):

    **MarriageDurationStep.tsx:**
    - Props: `{ onNext: () => void; onBack: () => void }`
    - react-hook-form + zodResolver로 `marriageDuration: z.string().min(1, '결혼 기간을 입력해주세요')` 검증
    - `useWizardStore`에서 `situation.marriageDuration`을 defaultValues로 사용 (INTAKE-03)
    - onSubmit 시 `updateSituation({ marriageDuration })` + `onNext()`
    - 질문 텍스트: "결혼하신 지 얼마나 되셨나요?"
    - input placeholder: "예: 5년, 10년 6개월"
    - min-h-[48px] 터치 타겟

    **ChildrenStep.tsx:**
    - Props: `{ onNext: () => void; onBack: () => void }`
    - RHF + Zod: `hasChildren: z.boolean()`, `childrenInfo: z.string().optional()`
    - 질문: "자녀가 있으신가요?"
    - hasChildren=true 시 추가 입력: "자녀 나이와 현재 양육 상황을 알려주세요"
    - defaultValues from wizardStore (INTAKE-03)
    - onSubmit: `updateSituation({ hasChildren, childrenInfo })`

    **AssetStep.tsx:**
    - Props: `{ onNext: () => void; onBack: () => void }`
    - RHF + Zod: `assetOverview: z.string().optional()`
    - 질문: "부부 공동 재산이 있으신가요? (부동산, 예금, 차량 등)"
    - textarea placeholder: "예: 아파트 1채(공동명의), 예금 5천만원 정도"
    - defaultValues from wizardStore
    - onSubmit: `updateSituation({ assetOverview })`

    **DivorceReasonStep.tsx:**
    - Props: `{ onNext: () => void; onBack: () => void }`
    - RHF + Zod: `divorceReason: z.string().min(1, '이혼 사유를 입력해주세요')`
    - 질문: "어떤 이유로 이혼을 고려하고 계신가요?"
    - textarea 자유 입력 + 공통 사유 빠른 선택 버튼들: "성격 차이", "경제적 문제", "외도/부정행위", "가정폭력", "기타"
    - 빠른 선택 클릭 시 textarea에 해당 텍스트 삽입
    - onSubmit 시:
      1. `detectDv(divorceReason)` 호출
      2. `setDvDetected(result)` 호출
      3. `updateSituation({ divorceReason })`
      4. `onNext()` (WizardContainer가 isDvDetected 확인 후 SafetyBranch 표시 결정)

    **SafetyBranch.tsx:**
    - Props: `{ onContinue: () => void }`
    - 배경: `bg-[#FFF3F0] border border-red-200 rounded-xl`
    - 제목: "먼저 안전을 확인해 주세요"
    - 설명: "입력하신 내용에 폭력 관련 상황이 포함된 것 같습니다."
    - 1366 전화 버튼: `<a href="tel:1366">여성긴급전화 1366</a>`, min-h-[48px], bg-red-600
    - 112 전화 버튼: `<a href="tel:112">경찰 112</a>`, min-h-[48px], bg-red-700
    - 계속 버튼: "안전한 상황입니다. 법률 정보 계속 보기", border-[#1B6B5A], min-h-[48px]

    **WizardContainer.tsx:**
    - `'use client'` 선언
    - Props: `{ onComplete: (situation: UserSituation) => void }`
    - `useWizardStore`에서 `currentStep`, `isDvDetected`, `situation`, `setStep` 사용
    - `showSafetyBranch` 로컬 state: step 4 제출 후 isDvDetected=true이면 SafetyBranch 표시
    - step 이동 로직:
      - `handleNext`: `setStep(currentStep + 1)`. step 4 완료 시 isDvDetected 체크 → SafetyBranch 또는 onComplete
      - `handleBack`: `setStep(currentStep - 1)`. step 0에서는 비활성화
    - SafetyBranch의 onContinue: `setShowSafetyBranch(false)` + `onComplete(situation as UserSituation)`
    - WizardProgress 포함: `current={currentStep}` `total={5}`
    - 조건부 렌더: switch(currentStep) { 0: IntroStep, 1: Marriage, 2: Children, 3: Asset, 4: DivorceReason }

    4. `npm test -- WizardContainer DivorceReasonStep` — GREEN.
  </action>
  <verify>
    <automated>cd C:/Users/hhc20/.openclaw/workspace && npm test -- WizardContainer DivorceReasonStep</automated>
  </verify>
  <acceptance_criteria>
    - src/components/wizard/WizardContainer.tsx에 `useWizardStore` import 포함
    - src/components/wizard/WizardContainer.tsx에 switch 또는 조건부 렌더로 5개 스텝 분기
    - src/components/wizard/DivorceReasonStep.tsx에 `import { detectDv } from '@/lib/dvDetector'` 포함
    - src/components/wizard/SafetyBranch.tsx에 `tel:1366` 포함
    - src/components/wizard/SafetyBranch.tsx에 `tel:112` 포함
    - src/components/wizard/MarriageDurationStep.tsx에 `useForm` import 포함
    - src/components/wizard/MarriageDurationStep.tsx에 `zodResolver` import 포함
    - 모든 스텝 컴포넌트에 `'use client'` 선언
    - `npm test -- WizardContainer DivorceReasonStep` 종료코드 0
  </acceptance_criteria>
  <done>5개 위저드 스텝 + SafetyBranch + WizardContainer가 구현되고 6개 테스트 GREEN. DV 감지 → SafetyBranch 분기 동작.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User wizard input → Store | 사용자 입력이 Zustand store에 저장 (메모리 전용, 검증 후) |
| Store → API call | situation 데이터가 /api/analyze로 전송됨 (서버에서 sanitize) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | WizardContainer | mitigate | localStorage/sessionStorage 미사용. Zustand 메모리 전용. 브라우저 종료 시 자동 삭제. URL에 위저드 데이터 미포함 (히스토리 노출 방지). |
| T-02-06 | Tampering | DivorceReasonStep | mitigate | 클라이언트 Zod 검증 + 서버 sanitizeInput 이중 검증. 2000자 제한은 서버에서 강제. |
| T-02-07 | Denial of Service | SafetyBranch | accept | SafetyBranch는 분석을 차단하지 않음. 사용자가 "계속 진행" 선택 가능. DV 감지 false positive는 UX 불편만 초래. |
</threat_model>

<verification>
```bash
cd C:/Users/hhc20/.openclaw/workspace && npm test -- WizardContainer DivorceReasonStep IntroStep
```
전체 위저드 관련 테스트 GREEN.
</verification>

<success_criteria>
- 5개 위저드 스텝(Intro → Marriage → Children → Asset → DivorceReason) + SafetyBranch + WizardContainer 구현
- 각 스텝이 react-hook-form + zodResolver 사용
- DV 키워드 감지 시 SafetyBranch 표시 후 계속 진행 가능
- 이전 스텝 이동 시 입력값 유지 (INTAKE-03)
- 10개 이상 테스트 GREEN
- 기존 Phase 1 테스트 깨지지 않음
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-ui-analysis-pipeline/02-03-SUMMARY.md`
</output>
